# Clause status — the FULL measured rows

> Moved VERBATIM out of `CLAUDE.md` on 2026-09-11 so the per-turn instruction file stays small (it had reached 744 KB / ~186k tokens). Nothing below was edited in the move; "above"/"below" refer to positions in the original file. Keep appending here, not in `CLAUDE.md`.

This is the complete `### Clause status, MEASURED` table as it stood in `CLAUDE.md` at `2984ad1` (2026-09-10). Rows (1), (4) and (6) were condensed in `CLAUDE.md`; this file keeps every measurement, correction and citation. Append new measurements HERE and update the one-paragraph row in `CLAUDE.md`.

### Clause status, MEASURED (do not re-derive these; each names its record)

| clause | state |
|---|---|
| (1) a backend zenavif can select **by default** | seam **works and is much wider at HEAD**. Closed at the seam 2026-09-08 (zenavif `e73e3d7`, `aae9d98`, `6788cdf`, `138cc01`): full pixel range, 4:4:4, alpha (the auxiliary Cs400 item), cq-0 lossless, GBR identity, and grayscale at 8/10/12. Three query surfaces now agree with the encode path by construction rather than by restatement — `validate_for_input` delegates to the encode path's own predicate, `backend_router` derives its matrix rule from the muxer's, and `zenavif-serialize` derives the whole `av1C`/`pixi`/`colr` from the payload's sequence header. Gated by `aom_encode_backend.rs` 24/24, `aom_roundtrip_loss.rs` 6/6 (72 support pairs + 24 routing configurations + 54 container/payload files) and `resolved_routing.rs` 6/6; whole-crate **449 passed / 0 failed** with `tests/vectors` provisioned. "By default" still needs two zenavif-side flips (`default` feature set; `#[default]` on `Av1Backend`), and **both remain blocked on clause (4) alone** — **BUT THAT SENTENCE ANSWERS THE BAR, NOT THE DEPLOYMENT QUESTION, AND THE TWO ARE DIFFERENT. MEASURED 2026-09-10: zenavif's CURRENT default encode backend is `Zenravif`, a rav1e fork (`zenavif/src/encoder.rs:262-265`, `#[default] Zenravif`, "production-proven") — NOT libaom.** So "can zenavif select this by default" is a zenav1-aom-vs-**zenrav1e** question at matched quality, while clause (4) is a zenav1-aom-vs-**libaom** RATIO. Both matter and they are not the same test: the ratio is the user's stated bar, the comparison against the incumbent is what decides whether flipping the default makes zenavif better or worse. **AND THE ONLY CROSS-ENCODER DATA PREDATES THE ENTIRE PERF PROGRAMME:** `benchmarks/xbench_2026-08-01.md` measures zenrav1e's qualifying mode at **speed 7, 1.82 MP/s** and the port at **cpu-used 6, 2.13 MP/s** / **cpu-used 3, 0.30 MP/s** — taken 2026-08-01, the day BEFORE KB-PERF-1 (10.66x -> 3.36x) and ~two cycles before the shipping preset reached 1.9443x. **Every zenav1-aom number in that table is stale by roughly 5x at the shipping preset, so the deployment comparison is UNMEASURED AT HEAD** — do not read xbench's 0.30 MP/s as current, and do not read this note as a claim that the port now wins. **IT IS NOW MEASURED — `benchmarks/encoder_vs_default_backend_2026-09-10.md` — AND THE ANSWER REVERSES THE 'BLOCKED' READING. At the SAME zenavif config, matched rate, identical photographic content, single-threaded both: zenav1-aom `encode_key_frame` cq27 cpu-used 3 = **2,994.6 ms / 40,237 B** against zenrav1e speed 4 q127 = **30,538 ms / 40,436 B** — **10.20x FASTER than the backend the flip would replace**, with rav1e's output 0.5 % LARGER so it is not buying time with a better rate point.** Speed 4 vs cpu-used 3 is ONE zenavif setting, not a handicap: `EncoderConfig::speed` defaults to 4, `backend_router.rs:754` passes `speed_effective()` straight through to rav1e while `encoder_aom.rs:248` maps the same 4 to cpu-used 3. **AND THE OLD TABLE CANNOT BE USED FOR THIS, for a SECOND reason beyond staleness: `drv-aom` times `port_encode_with` — the aom-bench DIFFERENTIAL HARNESS — not `encode_key_frame`. Measured on the identical cell emitting the identical 40,237 bytes: 5,687 ms vs 2,994.6 ms, a 1.90x inflation.** So xbench's zenav1-aom column understates the product by ~1.9x on top of being ~5x stale; fix that before re-running it for any product question. **THE TWO CAVEATS THAT BOUNDED THE 10x ARE NOW BOTH MEASURED, AND BOTH FELL THE PORT'S WAY** — they were recorded on 2026-09-10 as the honest ceiling on the claim, and leaving them written as open after measuring them would be the §9 failure in the perf log. **(a) QUALITY AT MATCHED RATE: the port WINS.** Both streams decoded with the pinned upstream `aomdec` and scored by `xtool score` against the source — zenav1-aom 40,237 B **SSIMULACRA2 79.22 / butteraugli max 3.435 / ba_3n 1.110** against zenrav1e 40,436 B **76.21 / 3.822 / 1.237**, i.e. **+3.01 SSIMULACRA2 at 0.5 % FEWER bytes** and better on both butteraugli metrics. So this is not speed bought with quality: the port is ahead on time, rate AND quality simultaneously, and at MATCHED quality the gap would widen (the port would run at a lower quality point). **(b) THREADED rav1e IS WORTH ~3 %, AND IT COSTS RATE.** `drv-rav1e` gained an optional 9th `threads` argument (tiles track threads — that is how rav1e parallelises inside one frame): **1 thread 30,777 ms / 40,436 B; 4 -> 30,682 / 41,459; 8 -> 30,110 / 42,049; 16 -> 29,836 / 42,853.** Sixteen threads buys **3.1 %** and costs **6.0 % more bytes**, taking the gap 10.28x -> **9.96x** while the rate match degrades faster than the time does. The mechanism is not mysterious: rav1e's main parallelism is FRAME-level, which a single-frame still cannot use at all, and intra-frame tiling both scales poorly at 1 MP and charges rate for the tile boundaries. Note the asymmetry — **the PORT spawns no threads at all** and has its own measured ~3.2x-at-4-threads headroom (`benchmarks/encoder_tile_threading_ceiling_2026-09-10.md`), untouched here. **WHAT STILL BOUNDS IT:** one content, one size, one rate point, one box; screen content deliberately excluded (KB-41's ~80 s/MP IntraBC search would answer a different question). **THE CONSEQUENCE: 'blocked on clause (4) alone' is true of the BAR and false as a statement about whether the flip would make zenavif worse. Quote both — 1.94x against the bar, ~10x against the incumbent — and note that the incumbent comparison is now a three-way win (time, rate, quality) that threading does not narrow.** Content for the re-run is emitted by `crates/aom-bench/examples/dump_cell_yuv.rs` (the committed corpus is screen-only). — at `--cpu-used 3`, which is the preset this seam actually ships (`zenavif/src/encoder.rs:506` `speed: 4`), where the ratio is **2.258x** at 1 MP. `benchmarks/zenavif_backend_integration_2026-09-08.md`. **RE-VERIFIED LIVE 2026-09-10 AGAINST THIS REPO'S HEAD, NOT THE PINNED REV — and that is the stronger check.** zenavif pins `zenav1-aom-encode` by git rev, so its own CI tests the pin; composing the sibling in with a temporary `[patch."https://github.com/imazen/zenav1-aom"]` path (the dev-only pattern zenavif's own `Cargo.toml` documents at lines 115-121, reverted immediately after) and running the seam: **`aom_encode_backend` 22/22, `aom_roundtrip_loss` 5/5, `resolved_routing` 6/6** — including `validate_agrees_with_the_encode_path`, `the_support_query_agrees_with_the_encode_path`, `the_router_query_agrees_with_the_encode_path`, `the_container_agrees_with_the_payload_on_every_format`, `aom_backend_every_speed_decodes`, `alpha_is_served_by_aom_in_place` and `twelve_bit_requirement_routes_to_aom`. **Do this whenever a `pub` signature in `aom-dsp`/`aom-encode` changes** — KB-PERF-57 changed one the same day, and the pinned-rev build could not have caught it. **AND KNOW THE SHAPE OF THE BREAKAGE BEFORE YOU ADD A FIELD: `zenavif/src/encoder_aom.rs:444` constructs `KeyFrameConfig` as a STRUCT LITERAL with no `..Default::default()`, so EVERY added field is a compile break there** — not a silent behaviour change, which is the better failure, but it means a field addition is a coordinated two-repo change and must be landed knowing that. `KeyFrameConfig::{enable_palette, enable_intrabc}` (2026-09-10) are the current instance: zenavif still builds against its pinned rev, and the two fields must be added to that literal at the next bump. **Their DEFAULTS are byte-inert on zenavif's own anchors by construction** — both are gated on the frame's screen decision, and the seam's byte anchors are photographic — but that is an argument, and the `[patch]` re-verify above is how it becomes a measurement. |
| (2) a support contract that never lies | `configuration_support.rs` + `refusal_census.rs` — the support query, the knob ranges and the documented refusals are asserted against the encoder's own behaviour. **RE-VERIFIED LIVE 2026-09-10 at HEAD: 14/14** across both files plus `encode_cancel`, including `the_documented_refusals_are_exactly_these` (pinned in BOTH directions), `every_format_and_depth_encodes_at_an_awkward_size`, `every_enumerated_knob_encodes_across_its_whole_range` (90 knob cells, 92 s) and `screen_shaped_tiny_cells_encode_rather_than_refuse` (60/60). |
| (3) no panics or refusals on reachable inputs | 90k fuzz inputs, 0 panics (`encode_fuzz_sweep.rs`); KB-51 was found this way. **RE-HUNTED AT HEAD 2026-09-10 — the committed default is only 600 inputs and the 90k figure predated ~60 commits: 45,000 inputs across seeds {1, 7, 101}, 13,315 of them reaching a REAL encode, 0 panics** (per seed ~4,440 encoded / ~5,770 unsupported / ~2,840 plane-size / ~1,950 sample-range, ~40 s each). All four non-vacuity classes fire, so the sweep is not testing `validate_configuration` and calling it panic-freedom. |
| (4) encode time within libaom | **NOT MET — ~1.943x at 1024x1024 `--cpu-used 3`, THE PRESET zenavif SHIPS.** **THE RATIO IS A CPU-TIME RATIO, NOT ONLY A WALL ONE, AND THE THREADING IS IDENTICAL — MEASURED 2026-09-10, because no band had ever checked it.** `/usr/bin/time -v` on the shipping cell (4 encodes per arm): port **12.01 s CPU / 12.02 s wall**, C **6.17 / 6.17**, **both 99 % CPU**, 1 voluntary context switch each. So CPU/wall is 0.999 and 1.000 and every band in this file is a CPU measurement as well as a wall one; the gap is not a scheduling artifact. **Threading is single on BOTH sides by construction and pinned three ways**: the oracle is built `-DCONFIG_MULTITHREAD=0` (`build.rs:414`, `reference/BUILD_CONFIG.md`) AND sets `cfg.g_threads = 1` at runtime (`dec_shim.c:498`), and the port spawns no threads anywhere (`grep` for `thread::spawn`/`rayon` in `aom-encode`/`aom-dsp` is empty). **STATE THE CONSEQUENCE RATHER THAN LEAVING IT IMPLIED: the comparison therefore EXCLUDES libaom's threading, which is a real product capability the port does not have. A threaded libaom would widen this gap on multi-core hardware and NOTHING in this repo measures that.** **MEMORY MANAGEMENT DIFFERS IN KIND, AND THE TWO METRICS POINT OPPOSITE WAYS.** Census re-taken at the SHIPPING cell for the first time (`eprof_alloc` now builds its cell the way the timing bands do — KB-PERF-13's named instrument gap, closed): one 1 MP encode at cq27 s3 makes **4,481,665 allocator calls** (3,510,848 `alloc` + 841,579 `alloc_zeroed` + 129,238 `realloc`), **2.05 GB cumulative**, **46.9 MB peak live**, **17,506 calls per superblock** — against libaom's recorded ~1.06 M (heaptrack, and note the counting `GlobalAlloc` CANNOT see libaom's own mallocs, so the C column must come from heaptrack, never from this instrument). But **peak RSS goes the other way: port 45.9 MB vs libaom 61.4 MB, and minor page faults 17,803 vs 28,701** — i.e. **the port makes ~4x the allocator calls in ~0.75x the footprint.** libaom preallocates large 32-byte-aligned per-frame workspaces (`aom_memalign`) and holds them; the port allocates millions of small short-lived buffers, mitigated but not removed by `SmallVec` inline storage (KB-PERF-43), stack arrays (KB-PERF-44) and the TLS scratch pools (KB-PERF-48/50). **This is why the allocation class is the one where the glibc bands systematically UNDERSTATE the port** — the same reduction is worth 6.9x more on Windows x86-64 at identical allocator censuses (`encoder_alloc_pass_windows_2026-09-10.md`). Two 8-round bands after the last two landings read **1.9443x** (port 3011.65 / C 1548.93) and **1.9432x** (port 3017.78 / C 1553.01), both arms 40,237 bytes. **THOSE TWO ARE THE SAME NUMBER: the C reference moved +0.26 % between them, so a separately-taken ratio carries ~±0.3 % and CANNOT see a sub-0.5 % landing — quote the PAIRED bands for individual levers and the ratio only for the headline.** (KB-PERF-55 measures −0.274 % paired at 20/24 p=0.0015 and is invisible in the ratio; that is the protocol working, not a contradiction.) **KB-PERF-56 — THE INTRA PREDICTOR WROTE INTO A SCRATCH AND THE CALLER COPIED THE BLOCK BACK: −0.497 % at the shipping preset (28/30, p=8.7e-07, null +0.080 %) and −0.793 % at 512x512 s0 (24/24, p=1.2e-07, null +0.042 %)** — `benchmarks/encoder_intra_pred_in_place_2026-09-10.md`. Byte-identical on four cells; gate 1507/1507 twice. **The ratio was NOT re-taken: a −0.5 % landing is inside the reference arm's own ±0.3 % drift, so the paired bands ARE the measurement** (the resolution caveat above, applied). The lever map annotated `intra_model_rd_y` and found its hottest single instruction is a per-row `memcpy` of u16 — the predict-into-a-tight-scratch-then-publish pattern — at the encoder's highest trip count, **per txb PER CANDIDATE MODE**. `predict_intra_high_in_place` is additive (each of the three highbd builders splits into a `plan_*` half that performs EVERY read of the reference plane into owned local arrays and a `write_*` half that touches no reference pixel, so reads complete before writes and aliasing cannot change a value); the two-slice entry points are untouched, keeping the DECODER out of the blast radius. **Five call sites converted, TWO deliberately not, and the distinction is the transferable half: `txfm_rd_in_plane_intra` and `encode_intra_block_plane_*` feed their prediction to `dist_block_px_domain` PER TX TYPE, which needs a tight `w*h` buffer — predicting in place there would MOVE the memcpy, not remove it.** So the win exists only where the prediction is consumed strided or not at all. **It pays MORE at speed 0 (−0.79 %) than at s3 (−0.50 %), which the mechanism predicts** (speed 0 evaluates far more candidates per txb) and which makes it a DIFFERENT overhead class from the per-transform-call setup that `encoder_ratio_vs_speed_2026-09-09.md` measured paying 4.6x LESS at the faster preset. **And it sharpens KB-PERF-51's null from the positive side: that landing removed two copies on a MINORITY of txbs and measured −0.147 % against a −0.077 % null; this removes one on EVERY prediction of EVERY candidate. Same class, same file, opposite verdict — cost the TRIP COUNT, not the bytes.** Gated by `predict_intra_in_place_diff`, which asserts the WHOLE PLANE (not the block) is byte-identical to predict-then-copy across every mode x angle-delta x filter-intra mode x 19 tx sizes x 6 availability combinations x bd {8,10,12}, and that the block still equals the real exported C predictor. **KB-PERF-57 — `aom_quantize_b_no_qmatrix` NOW WALKS RASTER ORDER, and the ranked "biggest single un-taken kernel left" turns out to be worth −0.134 % POOLED, not the +28.7 ms its 11.89x ratio suggests** — `benchmarks/encoder_quantize_b_raster_2026-09-10.md`. §14 held for once: re-profiled at HEAD the kernel is 0.86 % = 25.7 ms against the lever map's 26.9, and `objdump` confirms the shape (**217 instructions, ZERO ymm, ZERO xmm, 14 `cmp`, 8 panic-call sites, 2 `call memset@GLIBC`**). **THE DESIGN WAS DECIDED BY A COUNTER, NOT A GUESS, AND THE COUNTER IS THE DURABLE PART: 538,094 calls per 1 MP encode, mean n = 60.8, mean `non_zero_count` = 32.7, so `nzc/n = 0.538` — raster order pays 1.86x the ITERATIONS and has to buy that back.** It buys it back by deleting BOTH `memset`s (every position is now written unconditionally; C's pre-scan trims exactly `|coeff| < zbin[ac]` and C's main loop writes only where `|coeff| >= zbin[ac]`, **exact complements**, so the set of positions that WRITE is unchanged and only the set VISITED grew), every bounds check, the `scan[i]` load, the gather/scatter, and the per-coefficient class select (**C's `ac = (scan[i] != 0)` is asking whether the RASTER position is the DC — in raster order that is `i != 0`, so the DC peels off and all five per-class constants become loop scalars**). **eob = 1 + max(iscan[rc]) equals C's scan-order maximum because `iscan` inverts `scan` — the ONE order-sensitive output of this family (KB-12).** Not an invented design: `quant/simd.rs` already does exactly this for `quantize_fp`, and **libaom's own `aom_quantize_b_avx2` takes an `iscan` argument the `_c` body ignores, for precisely this reason.** **MEASURED: pooled over TWO independent 30-round bands, −0.134 % at 40/60, p=0.0135** (band A −0.169 % at 21/30 p=0.043, band B −0.106 % at 19/30 p=0.20; nulls −0.078 % / +0.037 %, pooled null −0.013 %) — **pooled because band A was marginal and B alone is not significant, and the pooled median sits BETWEEN the two band medians, so no favourable band was selected** (KB-PERF-36's protocol). That is **~4.0 ms of 25.7 = 16 % of the kernel. STATE IT AS A TRADE, NOT A REMOVAL: it removes two memsets, all bounds checks and the indirection but ADDS arithmetic on 0.46n extra coefficients, and the session's own rule says trades are where perf changes go wrong — this one measured positive and significant, but only just.** **The reason to keep it is structural: the kernel is now raster-order, sequential, unconditionally-storing and loop-invariant — the only shape a vector tier can take, and the shape libaom's `_avx2` already has; the scan-order form could not be vectorised at all.** **WHAT BLOCKS THE VECTOR TIER, so nobody re-derives it: `(u*32*quant)>>16` IS exactly `(u*quant)>>11` with `u*quant <= 2^30` (i32-safe) and `t2 = (u*quant>>11) + u*32 <= ~2^20.6` (i32-safe), but `t2 * quant_shift` reaches ~2^35 and is NOT.** libaom clears that wall with 16-bit arithmetic, which `CLAUDE.md` already rules STRUCTURAL for `quantize_fp` (bd8 `|dqcoeff|` reaches 33,137 against `i16::MAX` 32,767 — a margin of ONE unit), so a vector tier needs a derived bound on that product plus a runtime gate; and note the differential deliberately draws `quant_shift` INDEPENDENTLY of `dequant` (the KB-ARM-FLOAT root-#3 unrealizable-triple shape), so no bound may be assumed from how libaom builds its tables. **Bite proof aimed at the EOB and maximally asymmetric: deriving it from the RASTER index instead of `iscan` fails EXACTLY ONE test — `quantize_b_diff::quantize_b_differential_fuzz`, on the eob (`left: 16, right: 15`) with qcoeff and dqcoeff still matching — while 37 other quantize/txb tests stay green**, including `quantize_b_adaptive_diff`, `quantize_qm_diff` and `highbd_quant_diff`, whose kernels were deliberately left in scan order. The differential now inverts its random-permutation scan to build `iscan` and runs 240,000 cases against the real exported C. (re-measured at HEAD after KB-PERF-53: 8 rotated rounds, port 3011.65 ms / C 1548.93 ms, both arms 40,237 bytes; the C reference reproduces the previous band's 1549.47 to 0.03 %, so the delta is read against a live control). **Gap 1462.7 ms; the 1.5x bar needs 688.3 ms = 47 % of it.** **THE MULTITHREADING CEILING IS NOW MEASURED, AND IT DOES NOT MOVE THE RATIO — `benchmarks/encoder_tile_threading_ceiling_2026-09-10.md`.** The per-tile subtree (`pack_tile_lr_stop`) is **91.4-92.2 % of the encode**, stable across 1/4/16 tiles, so Amdahl gives **~3.2x at 4 threads and ~5.1x at 8** — against the **1.29x** that closing EVERY named lever completely would give. **But THREADING MOVES BOTH ARMS**: libaom threads too, and this repo CANNOT measure that (the oracle is built `-DCONFIG_MULTITHREAD=0` AND sets `g_threads = 1`), so at matched thread count the ratio stays ~1.94x. **Which bar is meant therefore decides the ranking: as a RATIO threading is NEUTRAL and does nothing for clause (4); as ABSOLUTE still-image wall time it is the largest lever in the project.** The bar is stated without a thread count and that ambiguity is now worth resolving. **The actionable half: ROW tiles are implementable with NO `unsafe`** — they own contiguous row bands of the recon plane, so `split_at_mut` suffices under `forbid(unsafe_code)`, where COLUMN tiles interleave within every row and do not; and row tiles are also CHEAPER, **4 row tiles costing +1.98 % bytes / +0.68 % serial time against 4 square tiles' +2.11 % / +1.96 %**. The per-tile loop already exists and is byte-exact (KB-31). Ceilings assume perfect load balance, which photographic tiles do not have; nothing here measures the imbalance. The 2026-09-10 cycle took this 2.258x -> 1.9443x. **AND THE LEVER MAP SAYS THE BAR IS NOT REACHABLE FROM THE NAMED LEVERS: closing EVERY one of them completely is +408.5 ms = 27 % of the gap** (`benchmarks/encoder_lever_map_s3_2026-09-10.md`), so the route is the reachability record's — halving every class gap, i.e. breadth — not a list of symbols. Historical, superseded: 1.962x, and before it 2.266x (`benchmarks/encoder_clause4_shipping_preset_2026-09-10.md`, re-measured at HEAD 2026-09-10: 8 rotated rounds, port median 3040.11 ms / C 1549.47 ms, spreads 0.79 %/1.41 %, paired per-round 1.9437-1.9750, **both arms 40,237 bytes on every round** so it is an RD-parity check as well as a timing). **The 2026-09-10 cycle took it 2.258x -> 1.962x, and the bar's requirement from 1174.7 ms = 60 % of the gap to 715.9 ms = 48 %.** Still not met; the reachability argument below (halving every class gap lands on 1.505x) is simply a smaller ask than when it was written. **Do NOT mix this with the `--cpu-used` 0 and 6 rows of the ladder — those were not re-taken and are older.** Historical, superseded: 2.266x at 1024x1024 `--cpu-used 3` (port 3493.19 / C 1541.3 ms, both arms byte-identical at 40,237 B; C measured twice 0.07 % apart in the same window). **The 2026-09-09 cycle's three kernel landings measured TOGETHER are −1.054 %, 33 of 33 rounds, p<0.0001** against a same-binary null of −0.048 % (`benchmarks/encoder_session_cumulative_2026-09-09.md`, 33 rotated rounds). **They compose at ~85 %: the arithmetic sum of their individual bands is −1.242 % (0.372 + 0.470 + 0.400) against −1.054 % measured together** — sub-additive because each landing shrinks the next one's denominator, so **quote the composed figure, never the sum** (playbook §6). That is **~37 ms of a ~1950 ms gap, and the bar needs ~1175 ms** — several more landings, and the largest remaining items are structural rather than kernel-shaped (see the u16-at-bd8 root below). Historical framing kept because the speed axis matters: **~2.34x at 1024x1024 `--cpu-used 0`, and SPEED 0 IS NOT THE PORT'S WORST CASE.** Measured 2026-09-09 at HEAD across the speed axis (512x512 cq27, `benchmarks/encoder_ratio_vs_speed_2026-09-09.md`): **s0 2.189x, s3 2.223x, s6 2.652x, s8 3.131x, s9 5.289x** — monotone, and the optimistic assumption (that a mid-speed shipping config would look better than the speed-0 headline) is REFUTED. Mechanism: as the preset rises libaom sheds search work while the port keeps its fixed per-call overhead, so that overhead becomes a larger fraction — which is exactly what every landing this cycle attacked, and implies they should pay MORE at s6+ (unmeasured). **Quote this ratio with its SPEED as well as its cell, and settle which preset zenavif ships at, because clause (1) reduces to this number at THAT speed.** **AND THE FOLLOW-UP REFUTED THE OBVIOUS PREDICTION:** this cycle's landings measure **-5.39 % at s0 but only -1.18 % at s6** (same two binaries, 20/20 and 24/24, byte-identical at both), i.e. they pay 4.6x LESS at the faster preset. There are **TWO** overhead classes with opposite speed behaviour — per-transform-call setup, which scales with the SEARCH and so is biggest at s0 (what this cycle removed), and a second class that scales with frame/block count and drives the ratio from 2.19x to 5.29x as the preset rises, which is **untouched and unidentified**. **THE PRESET zenavif SHIPS IS `--cpu-used 3`, MEASURED FROM THE INTEGRATION** (`zenavif/src/encoder.rs:506` default `speed: 4`; `encoder_aom.rs:248` `speed_to_cpu_used(s)=s-1`, one-to-one) **and it is the port's BEST cell: `benchmarks/encoder_shipping_preset_1mp_2026-09-09.md` measures 1024x1024 cq27 s3 at port 3498.73 / C 1549.38 / 2.258x / gap 1949.35 ms, byte-identical.** The 1 MP ladder at HEAD is **s0 2.337x, s3 2.258x, s6 2.696x — NOT monotone; it DIPS at the shipping preset**, which corrects `encoder_ratio_vs_speed_2026-09-09.md`'s headline (true of its 512x512 cell, false as a general claim). **Quote clause (4) with its speed as well as its cell: 2.258x at 1024x1024 --cpu-used 3.** Closing it needs **1174.66 ms of the 1949.35 ms gap (60 %)**. **The speed-0 ranking TRANSFERS to s3** — transform is 40.5 % of the s3 gap against 38.3 % of the s0 gap and is the #1 item in both, so this cycle's speed-0 work was well aimed; the one class that moves is loop-restoration (15.3 % -> 7.1 %). `optimize_txb_core` is FASTER than `av1_optimize_txb` for the THIRD consecutive profile (s3: 388.0 vs 415.2 ms) — do not spend a landing on it. **CORRECTION to the speed-6 profile taken the same day:** it measured the intra-mode CNN at 10.4x / +32.05 ms / **11.1 % of the s6 gap** (`benchmarks/encoder_speed6_profile_2026-09-09.md`; the class is invisible at speed 0 because `intra_cnn_based_part_prune_level` is 0 there, KB-23) and concluded one `cnn_avx2.c` port serves both clause (4) and KB-41 root #27. **At s3 the same CNN is +31.6 ms = 1.6 % of the gap** — its absolute cost is speed-invariant (once per 64x64, KB-PERF-1's cache) and only the denominator moved. **So port `cnn_avx2.c` for PARITY (KB-41 root #27, the last carrier of the datagen wave's 13 cells), not for clause (4).** General lesson: a lever's SHARE is a ratio — profiling at a speed the product does not ship inflates importance without changing the cost. Rank levers at the shipping configuration, or state the speed beside the share. **The transform census is now taken at the shipping preset too** (`benchmarks/encoder_txfm_size_census_s3_2026-09-09.md`), closing both gaps the speed-0 census named in itself: **the INVERSE size mix is measured for the first time and MATCHES the forward** (at s3 they agree within a point on every size), and the SPEED axis moves the distribution by ten points — **4x4 is 40.51 % of forwards at s3 against 50.70 % at s0**, 4x4+8x8 66.3 % not 73.0 %, `fwd_tx_non_dct` 61.66 % not 76.24 %, total forward calls 3.94x fewer. **So KB-PERF-16/17's fused 4x4 paths reach about a fifth LESS at the preset that ships than at the speed 0 they were measured at** — they are still the best-covered specialisation, but quote their reach with a speed. Sized against that census at s3: the transform DRIVER round-trip is **~181 ms with no C counterpart** (the largest structural item left); 8x8 fusion stays REJECTED (+7.07 %, size-structural so speed does not change it — do not retry); KB-PERF-9's named forward-ROW-pass `row_n == 4` gap is **only ~21 ms / 10.2 % of calls** (8x4 + 16x4; 4x4 is fused and bypasses the driver) and its row-major loads are the gather case that measured +9.8 % on the other pass, so it is small AND not obviously positive. Two candidates were ruled out by reading the profile: `run_inv1d_v3` 107.8 ms + `run_fwd1d_v3` 81.5 ms are inlined KERNEL BODIES, not dispatch overhead; and the i16 applicability scans fire on only ~10 % of calls. **BEFORE HAND-VECTORISING ANYTHING THAT LOOKS SCALAR, DISASSEMBLE IT — a scalar-looking SOURCE line is not scalar MACHINE CODE.** `benchmarks/encoder_pixel_proj_error_2026-09-09.rejected.md`: `pixel_proj_error`'s squaring tail (`for x in e.to_array() { err += x*x }`) was costed at ~25 uops -> ~6 and KB-PERF-6's declined bound was correctly refuted (that bound is on the SUM, `8*e^2 < 2^31`; the SQUARE alone needs only `e^2 < 2^31`, true with 8x margin, and i64-lane accumulation removes the sum bound entirely — integer addition is associative so it needs no gate). All correct, and all beside the point: **the BASE binary already had `vpmovsxdq=2 vpaddq=3` — LLVM had auto-vectorised that reduction on its own**, and the hand-written version moved 383 instructions to 376 and measured **−0.083 %, 28/48, p=0.31** against a LARGER null of +0.164 %. Not a harness miss: there was no scalar path to replace. **This session's three LANDINGS all replaced code the disassembly confirmed was scalar** (`filter_intra` ymm=0, `hadamard_8x8` ymm=0, `wiener` vpmaddwd=0); this one did not, and one `objdump` before writing would have shown it. Residual: `pixel_proj_error` stays 23.4 vs 7.9 = 2.97x, and since the squaring is already vectorised the gap is in the `e` computation — i32x8 (8 lanes) against libaom's i16 (16 lanes) on u8 source, i.e. the u16-at-bd8 root, not reachable from inside the function. **THE SYSTEMATIC CAUSE IS NOW MEASURED AND IT IS LANE WIDTH** (`benchmarks/encoder_simd_lane_width_audit_2026-09-09.md`). Restricting the comparison to operations where BOTH sides are already vectorised, the port is **3-7.5x slower in NINE of them, together +391 ms = 20 % of the shipping-preset gap**: directional intra +100.7, quantize_fp +54.6, **filter_intra +47.3 at 7.50x**, loopfilter +44.8, SGR +36.2, wiener +34.7, txb_init_levels +30.8, hadamard +26.5, pixel_proj_error +15.5 (trellis is the one the port WINS, 0.93x). **Coverage is NOT the problem** — libaom spends 32.5 % of its wall in explicit-ISA symbols and the port spends 28.2 % in magetypes vector bodies. **The cause is that the port's kernels are defined over WIDER lane types, so same register width, fewer elements per instruction.** Verified in the shipped binary's disassembly: `lpf_impl_v3` is **insns=2603 ymm=0 xmm=1056** — its `v3` body emits NO 256-bit instruction, because `define(i32x4)` is a 128-BIT type; libaom's loopfilter is `_sse2` too, but puts **16 u8 elements** in that register where the port puts **4 i32**, and 4x lanes predicts 4.57x measured. Same for quantize_fp (i32x8=8 vs i16 avx2=16) and wiener (i32x8=8 vs i16 avx2=16). This is `CLAUDE.md`'s existing u16-at-bd8 root restated as a LANE COUNT, measured across nine operations at the preset that ships — and it explains why the two landings that narrowed a lane type paid (KB-PERF-3's i16 forward, KB-PERF-5's u16x16 SMOOTH) while every lane-width-NEUTRAL attempt measured null. **THREE OF THE NINE ARE NOT BLOCKED AFTER ALL — MEASURED BY COMPILING** (`benchmarks/encoder_intrinsics_unblock_2026-09-09.md`). The vocabulary claim is TRUE at the pinned magetypes **0.9.29** (no integer interleave, no integer transpose, no `mul_high`; interleave/transpose exist for **f32 only**) — but the CONCLUSION was false: `archmage::intrinsics::x86_64` re-exports `core::arch`, and a `#[rite(v3)]` body carries `target_feature(avx2)`, so under target-feature-11 **raw AVX2 intrinsics compile inside `aom-dsp` WITH `#![forbid(unsafe_code)]` in force** — verified by a probe calling `_mm256_unpacklo_epi16` and `_mm256_mulhi_epi16` (rustc 1.98.1; probe removed, nothing shipped). **So `quantize_fp` (+54.6 ms, needs `_mm256_mulhi_epi16`), `wiener` (+34.7, needs `_mm256_unpacklo_epi16`) and `hadamard` (+26.5, needs the unpack family) are all reachable — +115.8 ms = 5.9 % of the gap — each using the SAME instruction libaom's own AVX2 kernel uses.** **HADAMARD IS NOW LANDED — KB-PERF-21, `benchmarks/encoder_hadamard_avx2_2026-09-09.md`: −0.470 % at 1024x1024 cq27 s3, **24/24 rounds**, p<0.0001, against a same-binary null of +0.143 % that runs the OTHER way; byte-identical on four cells; gate 1506/1506 in both dispatch modes.** It is the **first landing to mix raw intrinsics into a magetypes body** and is the pattern for the two that remain. `hadamard_8x8` was 31.1 ms vs `aom_hadamard_8x8_sse2` 7.9 = 3.94x; vectorised with lane=column — contiguous row loads, the `hadamard_col8` butterfly across eight `__m128i` (`_mm_add_epi16`/`_mm_sub_epi16` wrap, which IS `i16::wrapping_add/sub`), ONE three-stage in-register 8x8 i16 transpose to make pass 2 vertical, then `_mm256_cvtepi16_epi32`. Emitted: **24 vpaddw + 24 vpsubw + 24 vpunpck* + 8 vpmovsxwd**. Measured −0.470 % = ~16.5 ms of the +23.2 ms gap = **71 %, the best conversion of the three kernels tried** (filter-intra ~55 %, the edge filter ~25 %). **Bite proof is maximally asymmetric: swapping two lanes of the transpose's last stage fails EXACTLY ONE test — `hadamard_diff::hadamard_satd_byte_identical` vs the real exported C — while 394 stay green**, which matters because KB-12 is the standing proof that a lost transpose HERE moves the `eob` alone and reads as an RD near-tie. x86-64 only; aarch64/wasm keep the scalar core, and `hadamard_4x4` is untouched (its `hadamard_col4` widens to i32 before narrowing — a different proof). **WIENER IS NOW LANDED TOO — KB-PERF-22, `benchmarks/encoder_wiener_madd_2026-09-09.md`: −0.40 % at 1024x1024 cq27 s3** (mean of two base copies, −0.247 % at 32/48 p=0.029 and −0.553 % at 37/48 p=0.0002; the spread between them IS the ~0.27 pp copy systematic `encoder_rotate_reverify_2026-08-03.md` documents, so **quote −0.40 % ± 0.15 pp, NOT the flattering −0.553 %**), 48-round rotated band, byte-identical on four cells, gate 1506/1506 in both dispatch modes. **It landed exactly where it was costed**: `encoder_wiener_uop_analysis_2026-09-09.md` predicted −0.3 to −0.4 % for the 16-column shape BEFORE any code was written, and warned the obvious 8-column madd shape would measure null (32 -> 27 uops, under this box's resolution) — a trap two earlier kernels this session fell into. Both wiener passes share one shape (`out[c] = clamp(round(bias + (in[c+3*step]<<7) + sum_k in[c+k*step]*tap[k]))`, `step` = 1 sample horizontally / one row vertically), so ONE helper serves both: two loads `step` apart, `unpacklo`/`unpackhi` to interleave, `vperm2i128` to straighten AVX2's per-128-lane result, then **8 `vpmaddwd` for 16 output columns**, `vpackusdw`+`vpermq` to store. Emitted: 4 vpunpcklwd + 4 vpunpckhwd + 4 vperm2i128 + 8 vpmaddwd + 1 vpackusdw + 1 vpermq, **no vpmulld**. **NO range obligation, and that was PROVEN not gated** — madd takes i16 in and accumulates in i32, horizontal inputs are ≤ 4095 and `conv_params_wiener` pins the vertical clamp ceiling at exactly 32767 = i16::MAX at every bit depth. Bite proof asymmetric (4 restoration tests fail incl. `wiener_convolve_matches_c` vs real exported C, 391 green). `w < 16`, aarch64 and wasm keep the i32x8 path; **the residual ~2x is libaom's u8 source — 32 samples per load with no lane fix — i.e. the u16-at-bd8 root, not reachable from inside a convolve.** **ALSO FOUND AND NOW FIXED (in part) — `cdef_find_dir_simd_diff` was genuinely FLAKY, and it is a REGRESSION FROM THE TEST CONSOLIDATION.** `archmage::testing::for_each_token_permutation` enables/disables dispatch tokens **process-wide**, 13 modules of the consolidated `aom-dsp` binary call it, and `cargo test` runs a binary's tests on an intra-binary thread pool — so sweeps overlap and each sees the other's token state (symptom: a kernel takes the SIMD path while the harness believes the tier is scalar). **Before `a88e739` ("consolidate 320 test binaries into 7") every `tests/*.rs` was its own PROCESS, so this could not happen**; that commit's own doc names the mechanism (*"lets `cargo test`'s intra-binary thread pool run ALL of these concurrently"*) without noticing global dispatch state is not thread-safe to permute. **SCOPE, MEASURED:** `cargo test` multi-threaded **3 failures in 15 runs**; `cargo test -- --test-threads=1` **0 in 10**; **`cargo nextest` (process per test) 0 in 10 — so `just gate-landing` was NEVER affected** and this is a local-`cargo test` hazard only. Fixed by `crates/aom-dsp/tests/all/dispatch_serial.rs`, a process-local mutex taken by all 14 sweep sites (same class as KB-50's counting-allocator mutex and KB-48's `timing_serial()`). **It closes the sweep-vs-sweep race ONLY, and the residual is stated rather than papered over**: a sweep also races any concurrent test that merely READS dispatch state by calling a kernel, and closing that would need an `RwLock` across hundreds of tests — disproportionate to a hazard both supported runners already avoid. **If you hit it locally, use `--test-threads=1` or `cargo nextest`.** — run ALONE three times with no code change it failed 1 of 3, which corrects BOTH KB-PERF-20's note ("reproducible") and KB-PERF-21's ("two of three perturbations"). A non-deterministic differential is a GATE HOLE: it can mask a real regression, and here it cost time by implicating an unrelated change. Worth its own fix. **REMAINING of the three: quantize_fp ONLY — hadamard and wiener are both landed (KB-PERF-21/22).** **And it is NOT blocked on `mul_high`** (`benchmarks/encoder_quantize_semantics_2026-09-09.md`): that instruction is available AND is the SAFE part (`abs_r` is already clamped into i16 and `quant` is i16, so `vpmulhw` reproduces `(abs_r*quant)>>16` exactly at every bit depth). **The unsafe multiply is the OTHER one, which nobody had named**: libaom's two `vpmullw` compute `dqcoeff = tmp * dequant` in 16-bit WRAPPING arithmetic where the port uses i32, and `tmp` reaches 32767 x dequant ~1800 = ~59 M, far outside i16. The port is byte-identical to aomenc on 427/427 cells, so they coincide on that envelope — but KB-20 measured they do NOT always (NEON truncates, AVX2 saturates). **So converting is not 'make it faster', it is choosing WHICH libaom to be bit-identical to, and it would make the port's own bitstream ISA-conditional.** The gate settles it: `just gate-landing` runs the suite TWICE, and a 16-bit vector tier that wraps where the scalar transcription does not CANNOT pass both legs on any overflowing cell — so an i16 rewrite is admissible only WITH a proof that overflow is unreachable, a domain proof over `(coeff, quant, dequant, log_scale)`. **AND THE PREREQUISITE IS NOW ANSWERED: THE OVERFLOW IS REACHABLE** (`xtask/audit_quantize_dqcoeff_range.py`, computed from the port's own quantizer tables). Because `quant_fp = 65536/dequant`, `tmp*dequant ~ |coeff|`, so the bound tracks the FORWARD-TRANSFORM cap: worst `|dqcoeff|` is **33,137 at bd8 (1.01x over i16::MAX), 131,870 at bd10, 134,937 at bd12**. At bd8 — the only depth `av1_quantize_fp_avx2` serves — **the margin is ONE UNIT**: `av1_gen_fwd_stage_range` caps `|coeff|` at 2^15 = 32768 (KB-ARM-FLOAT root #3 gets the same number from the spec clamp) against i16::MAX 32767. **So an i16 rewrite CANNOT be justified by unreachability, and `gate-landing`'s two legs would reject it on any cell that reaches the cap. STOP RANKING quantize_fp's +54.6 ms as an available lever** — it is structural in the same sense as directional intra. **Do not implement it as a perf task**; the decision (stay `_c`, ISA-neutral, or match the dispatched `_avx2`) has KB-ARM-FLOAT root #1 as this project's precedent, and if the answer is 'stay `_c`' then +54.6 ms is largely STRUCTURAL and should stop being ranked as an available lever. Instruction mix, for the record: port 365 insns all `*d` (32-bit); C 122 insns all `*w` (16-bit). **Also: the audit's LOOPFILTER row carried the same asymmetric-pairing error as its filter_intra row** (it counted the port's `loop_filter_frame_stop` but no C frame driver); like for like the kernel is **44.8 vs 12.5 = 3.58x, +32.3 ms**. And its `define(i32x4)` is **NOT wasted width** — one `aom_lpf_*` call filters exactly 4 edge positions and those ARE the 4 lanes; the real gap is that libaom uses `_dual` (8 positions) and `_quad` (16) on u8 data, so closing it needs the frame driver to BATCH adjacent segments as well as a narrower type — a programme, not a lever. **The two ranked levers it produces: (1) `filter_intra` — **CORRECTION: that audit row was OVERSTATED at +47.3 ms by an asymmetric pairing** (the port side summed the predictor AND the edge filter, the C side only the predictor); like for like it is **predictor 31.1 vs 7.3 = 4.26x, +23.8 ms** and **edge filter 20.3 vs 5.6 = 3.63x, +14.7 ms**. **The PREDICTOR half is now LANDED — KB-PERF-20, `benchmarks/encoder_filter_intra_simd_2026-09-09.md`: −0.372 % at 1024x1024 cq27 s3, 22/24 rounds, p<0.0001, against a same-binary null of −0.041 %, byte-identical on four cells, gate 1506/1506 in both dispatch modes.** One 4x2 sub-block is 8 outputs and the vector is 8 i32 lanes, so NO idle lanes — 56 scalar multiplies become 7 vector multiply-adds over a `const fn`-transposed tap table; bit-exact by construction (same expression per lane, same accumulation order) so it needs no bound and no runtime gate. The tier is held across the WHOLE loop nest (dispatch once per predict, not per 4x2 block), verified by disassembly: **547 insns / 0 ymm -> 355 insns / 69 ymm / 7 `vpmulld`**. **The EDGE FILTER was then BUILT AND REJECTED** (`benchmarks/encoder_filter_intra_edge_2026-09-09.rejected.md`): correct, byte-identical, bite-proved on its cross-batch carry (4 differentials fail, 391 green), and measured **+0.058 %, 9/24, p=0.31** against a same-binary null of **+0.120 %**. **Not a harness miss — the work moved and DID get faster**: scalar `highbd_filter_intra_edge` 20.3 -> 5.3 ms plus a new `filter_intra_edge_impl_v3` 11.3, i.e. 20.3 -> 16.6, a saving of only **~3.7 ms = 0.11 %**, at this band's resolution floor and nowhere near C's 5.6 ms. **Why, and it is the durable lesson: `sz` is 9..65, so a batch of 8 runs one to seven times and the per-batch setup dominates.** KB-PERF-20's predictor paid because its unit of work is a 4x2 block repeated across a whole transform; the edge filter's unit is a single short 1-D array with nothing to amortise vector width over. **A kernel with a real ISA counterpart in libaom is not automatically a lever — check the TRIP COUNT, not just the ratio.** Also confirmed there, independently and at symbol level: KB-PERF-20 took `filter_intra_predict_high` **31.1 ms scalar -> 13.4 ms vector, 4.26x -> 1.84x**. And a CORRECTION to KB-PERF-20's own closing note: `cdef_find_dir_simd_diff` failing under unrelated perturbations is **flaky (2 of 3), not reproducible** as that note claimed; (2) `loopfilter` +44.8 ms, `define(i32x4)` -> a narrower type, and unlike wiener (blocked on integer interleave) and quantize_fp (blocked on `mul_high`) its ops — add/sub/abs/min/max/compare/blend — are all in magetypes for narrow types.** **REJECTED this session, with its mechanism: the forward ROW pass `row_n == 4` arm** (`benchmarks/encoder_fwd_row_4wide_2026-09-09.rejected.md`) — KB-PERF-9's own named gap, built, byte-identical, bite-proved reached (3 differentials fail, 391 stay green), and measured NULL at both cells (+0.085 % at 512 s0 p=0.54; -0.065 % at 1024 s3 p=0.54, where the same-binary null is LARGER at -0.155 %). **It is NOT a harness miss and that was checked directly**: profiling both binaries shows the scalar row kernels go **0.59 % -> exactly 0.00 %** while `fwd_row_pass_core_v3` grows **1.31 % -> 2.14 %**, i.e. the path changed completely and the trade is a net **+0.24 %**. Mechanism: the row pass loads through an 8x8 register TRANSPOSE, which at `row_n == 4` runs at full cost on half-zero data — the column pass has no transpose, which is why the identical half-batch shape paid there. **Do not re-attempt without removing the transpose.** Also note magetypes is at **0.9.29** in `Cargo.lock` (not 0.9.28 as several entries say), so `narrow_saturating_i32_to_i16`/`_i16_to_u8` DO exist — but `txb_init_levels` emits 8-byte runs for 89 % of calls (height 4 and 8), which a 32-byte pack cannot store directly, so that follow-up is smaller than it looks; `mul_high` is still absent. Historical: ~2.39x (KB-PERF-16/17 fused 4x4 fwd+inv, KB-PERF-18 stack-buffers the inverse intermediate, KB-PERF-19 drops the quantize scratch's redundant `clear()`. **Two rules were paid for here. (a) Fuse only where the call count is high AND the generic SIMD declines or runs half-filled — an 8x8 fusion measured +7.07 % and was REVERTED, because a fused kernel is scalar and loses to a full 8-wide pass; the synthesis is to remove the driver's SETUP and keep its PASSES. (b) A reverted-as-null perf change is refuted for its measured CELL, not for the codebase — KB-PERF-19 is KB-PERF-2's own skip-the-re-zero variant, null on aarch64/cpu6 and -0.72 % (24/24) on x86-64/cpu0, exactly the platform-dependence KB-PERF-2 predicted.**) Historical: ~2.39x (KB-PERF-16/17 fused 4x4 forward+inverse, then KB-PERF-18 stack-buffers the inverse intermediate for the 86.7 % of calls needing <= 64 coefficients, -0.73 % 24/24. **The governing rule, paid for by an 8x8 fusion that measured +7.07 % and was REVERTED: fuse only where the call count is high AND the generic path's SIMD declines or runs half-filled — a fused kernel is scalar, so against a full 8-wide pass it LOSES. KB-PERF-18 is the synthesis: remove the driver's per-call setup and KEEP the SIMD.** Its symmetric forward twin was built, measured indistinguishable (-0.70 % 20/24 vs -0.73 % 24/24) and rejected, because 4x4 already bypasses the forward scratch.) Historical: ~2.39x (KB-PERF-16 fused-4x4 FORWARD + KB-PERF-17 fused-4x4 INVERSE together take the 1 MP cell **10051.6 -> 9820.8 ms, -2.30 %**, and 512x512 **-3.53 %**; ratio **2.410x -> 2.355x** against one C median. Both were chosen by census, not by kernel size — 4x4 is **50.70 % of forward** and **44.43 % of inverse** transforms while everything with both dims >= 32 is ~0.5 % combined. The lever is PER-CALL OVERHEAD, so KB-PERF-3's fadst4 i16 rejection does not apply. **The two recipes collapse DIFFERENTLY — `FWD_SHIFT[TX_4X4]=[2,0,0]` vs `INV_SHIFT[TX_4X4]=[0,-4]`, i.e. the forward's no-ops are the inverse's live shift — so copying one argument to the other would be wrong.** Next: 8x8 at 22.3 %/25.7 %, where `INV_SHIFT[TX_8X8]=[-1,-4]` makes BOTH shifts live.) Historical: ~2.39x (KB-PERF-16 fused-4x4 2.410x -> **2.372x** in its own band, -1.55 % paired at 1 MP and -2.15 % at 512x512, the largest single lever of the 2026-09-09 cycle and the FIRST landing inside the transform class; it was chosen by the census `benchmarks/encoder_txfm_size_census_2026-09-09.md`, which measured **4x4 = 50.70 % of all forward transforms** while everything with both dims >= 32 is 0.55 % combined — the big kernels a whole-transform rewrite starts with are worth nothing here, and the lever is PER-CALL OVERHEAD rather than lane width, so KB-PERF-3's fadst4 i16 rejection does not apply.) Historical: ~2.39x (KB-PERF-9 2.521x -> 2.478x, KB-PERF-10 2.488x -> 2.464x, KB-PERF-11 2.457x -> 2.440x, KB-PERF-12 2.430x -> 2.424x, KB-PERF-13 2.410x -> 2.398x, KB-PERF-14 2.402x -> 2.395x, all at 1 MP. **Each ratio is against a C median from its OWN band and the C arm drifts 1.15 % across those four bands, so every ratio carries ~±0.6 % and the chain is not as precise as its digits — quote the PAIRED port-vs-port deltas when precision matters**; the 192x192 cell has not been re-measured since KB-PERF-8's 2.475x, so do not quote a 192x192 figure for either) (2.542x / 2.624x before KB-PERF-7, 2.69x before KB-PERF-6) vs Gate 3's <= 1.5x; the 3.24x-4.03x band across byte-identical cells is `benchmarks/encode_perf_vs_libaom_2026-09-08.md`. **This is the critical path: clause (1) reduces to it, and closing it needs ~178 ms of a 275 ms gap — several landings, not one.** **Rank levers off `benchmarks/encoder_x86_reprofile_1024_2026-09-09.after.md`** — the table re-taken at HEAD after KB-PERF-9..15 (port 10128 / C 4222 / **2.399x** / gap 5906 ms): transform +2263 (38.3 %), rd-driver +950 (16.1 %), loop-restoration +901 (15.3 %), intra-pred +473, txb/trellis +464 at **1.31x**, memory +448 at 7.43x, quantize +384 (never examined, and the one class that GREW), distortion +234. Its opening table is in `benchmarks/encoder_x86_reprofile_1024_2026-09-09.md` (2026-09-09, taken at 1024x1024 AFTER KB-PERF-7/8 — the 192x192 tables are L2-resident and their class SHARES do not carry to a 1 MP frame; it supersedes `..._x86_reprofile_2026-09-08.md`, which supersedes `..._x86_profile_...`). **Its headline finding is a lever that does NOT exist: `optimize_txb_core` is the port's LARGEST symbol at 11.44 %, and like-for-like it is 1211 ms against C's `av1_optimize_txb` 1258 ms — the port's trellis is FASTER, and the whole txb/trellis class runs at ratio 1.32, the best of any class. Do not spend a landing on it.** The class table at 1 MP (gap 6370 ms): transform +2440 (38.3 %), rd-driver +1023 (16.1 %), loop-restoration +900 (14.1 %), intra-pred +514, memory/memset +486 at 12.4x, txb +483 at 1.32x, quantize +340, distortion +295. Every KB-PERF-1..5 ranking is aarch64-apple-darwin and does NOT transfer. **CORRECTED 2026-09-09 — THE u16-PLANE ROOT IS 24.4 % OF THE SHIPPING-PRESET GAP, NOT ~42 %, AND 52.8 % IS COEFFICIENT-DOMAIN WHERE IT CANNOT HELP** (`benchmarks/encoder_domain_split_2026-09-09.md`; the ~42 % below is from a 192x192 aarch64 profile). Bucketing every symbol of both s3 profiles by whether it touches plane SAMPLES or COEFFICIENTS: **pixel +474.9 ms = 24.4 % (2.77x), coefficient +1028.8 ms = 52.8 % (2.23x)**, RD-driver inlining sinks +311.8 = 16.0 % (unattributable, KB-PERF-10), other +129.4 = 6.6 %. **`u8` halves the DATA, not the time**, so the plane refactor's realistic value is ~half of its 24.4 % ceiling — **~12 % of the gap for an encoder-wide change to `KeyFramePlanes`/`ReconPlane` and every predictor, filter and restoration call site.** Meanwhile the **transform class alone is +789 ms = 40.5 %** and is purely coefficient-domain. **AND THAT LANDING IS DONE — KB-PERF-23, `benchmarks/encoder_fused_txfm8x8_simd_2026-09-09.md`: the SIMD-preserving fused 8x8 FORWARD transform, **−2.017 % at 1024x1024 cq27 s3, 24 of 24 rounds, p<0.0001**, against a same-binary null of +0.128 %; byte-identical on four cells; gate 1506/1506 in both dispatch modes. **That is TWICE this cycle's three previous landings combined (−1.054 %)**, ~71 ms, inside the 60-125 ms predicted before the work started. It keeps BOTH vector passes and removes only the DRIVER (`get_fwd_txfm_cfg`, the two `try_*` gates, the scratch tiering, and the `buf` round trip): lane = COLUMN for pass 1, ONE in-register 8x8 i32 transpose to make lane = ROW, then pass 2 and a contiguous vector store per output row. **The transpose is not an addition** — the generic row pass already pays one loading `buf` through 8x8 tiles; this moves it out of memory. Exactness: `FWD_SHIFT[TX_8X8] = [2,-1,0]` read from the table, `shl_clamp64v(_,2)` and `rshiftv(_,1)` are the two `round_shift_array` calls, shift[2]==0 and rect==0 so no tail, and **`lr_flip` is a lane REVERSE applied AFTER the column kernel** (the generic writes lane j to column col_n-1-j) — that is the bite proof. **The differentials earned their keep TWICE**: the first transpose was index-computed and WRONG (stage 2 paired `lo(t1,t3)` where it needs `hi(t0,t2)`), caught immediately by `txfm2d_differential_fuzz` + `txfm2d_simd_perm_diff`, and rewritten as explicit named stages — a clever index expression is not worth its debugging cost in a transpose. **THE INVERSE 8x8 IS NOW LANDED TOO — KB-PERF-24, `benchmarks/encoder_fused_inv_txfm8x8_simd_2026-09-09.md`: −1.538 % at 1024x1024 cq27 s3, **24 of 24 rounds**, p<0.0001, against a same-binary null of −0.035 %; byte-identical on four cells; gate 1506/1506 in both dispatch modes.** It DID need its own proof: `INV_SHIFT[TX_8X8] = [-1,-4]` makes both shifts live (the forward's `[2,-1,0]` has a no-op tail) and the inverse carries two `clamp_buf` calls plus the `highbd_clip_pixel_add` reconstruction. **One structural advantage the other way**: the inverse input is COLUMN-major (`mod_input[c*row_n + r]`), so the ROW pass needs NO transpose — a contiguous 8-lane load already has lane = r, and the single transpose moves to between the passes where the generic pays it anyway. **`ud_flip` REORDERS the eight output vectors; `lr_flip` is a LANE REVERSE on the transposed ones — conflating them is the easy mistake.** **The differentials caught the one real defect: `opt_range(bd)` returns `(COL, ROW)`** and the first version fed the col range to the row pass; the call site now names the bindings `opt_col`/`opt_row`. **That is the SECOND consecutive landing whose only defect was a tuple/index ORDER slip, both found in one run by the differentials against the real exported C** (KB-PERF-23's was an index-computed transpose stage) — write those out explicitly rather than computing them. **AND 16x16 FORWARD IS LANDED — KB-PERF-25, `benchmarks/encoder_fused_txfm16x16_simd_2026-09-09.md`: −0.600 % at 1024x1024 cq27 s3** (mean of two base copies, −0.706 % at 27/30 and −0.493 % at 26/30, both p<=0.0001; **the null is significant in the SAME direction at −0.238 %**, the ~0.27 pp copy systematic, so the raw −0.706 % would overstate — quote the mean, as KB-PERF-22 does), byte-identical, gate 1506/1506 both modes. **ABOVE the −0.44 % predicted**, and the '3x the code' estimate was pessimistic: factoring the 8x8 transpose into one closure makes 16x16 **two vectors per row, four transpose calls, two kernel calls per pass** — additive, not multiplicative. The four 8x8 block transposes land exactly on the two row-groups the row pass consumes, so **no off-diagonal swap is needed**. **Two things differ from 8x8 and both were read from the tables rather than copied: `FWD_SHIFT[TX_16X16] = [2,-2,0]` (shift1 is 2, not 1), and `COS_BIT_ROW[2][2] is 12 while COS_BIT_COL[2][2] is 13** — at 8x8 both are 13, so reusing a single `cos_bit` would have been silently wrong on the row pass. **`lr_flip` is a half-SWAP plus a lane reverse** (source column c -> 15-c), where at 8x8 it was a plain reverse — that is the bite proof. **Green on the FIRST run**, unlike KB-PERF-23/24 whose only defects were an index-computed transpose stage and a swapped `opt_range` tuple; writing both out explicitly this time is why. **THE INVERSE 16x16 IS LANDED — KB-PERF-26, `benchmarks/encoder_fused_inv_txfm16x16_simd_2026-09-09.md`: −0.480 %** (mean of two base copies, −0.586 % at 28/30 and −0.374 % at 26/30, both p<=0.0001; null −0.226 %, same direction), byte-identical, gate 1506/1506, **green on the first run** — the sequence is mechanical by this point. Bite proof on the axis that is easy to confuse: **`ud_flip` REORDERS the sixteen output vectors while `lr_flip` is a lane operation**; dropping the reorder fails FOUR tests (three against the real exported C) while 391 stay green. **THE SEQUENCE, COMPLETE: 8x8 fwd −2.017 % (25.74 % share), 8x8 inv −1.538 % (26.96 %), 16x16 fwd −0.600 % (5.62 %), 16x16 inv −0.480 % (5.04 %). Both 16x16 kernels BEAT their call-share prediction** (−0.44 % and ~0.3 %) — larger transforms cost more per call so the driver saving is a smaller fraction, but there is more `buf` traffic to remove and that appears to dominate. **MEASURED SAME-WINDOW AFTER ALL FOUR: port 3331.01 ms / C 1553.8 = 2.144x at the shipping preset, against 2.281x at this cycle's start** (C twice, 0.34 % apart) — the cycle is **−6.00 %** on the port arm. **8x4 + 4x8 FORWARD IS NOW LANDED — KB-PERF-27, `benchmarks/encoder_fused_txfm_rect48_2026-09-09.md`: −0.804 %** (mean of two base copies, −0.891 % at 28/30 and −0.716 % at 27/30, both p<0.0001; null −0.165 %, same direction), byte-identical, gate 1506/1506, green first run. **The largest of the four landings after the 8x8 pair, exactly as its 16.7 % share predicted.** ONE body serves both shapes (both fit in eight vectors of eight lanes with some lanes/vectors unused; padding is zero and unread). Two things differ from the square kernels: **`rect_type = ±1` so the row pass applies the `NEW_SQRT2` scaling they skip** (that is the bite proof), and **`lr_flip` is an ARRAY-ORDER reversal, which is SIMPLER than what the square kernels do** — the generic column pass permutes which column POSITION each source column occupies, and after the transpose `t[c]` already holds source column `c`, so feeding the row pass `t[col_n-1-p]` is the same permutation at zero cost; KB-PERF-23/25 reverse lanes, equivalent there but **a future size should use the array-order form.** Its hook also takes the config from **`get_fwd_txfm_cfg` itself** rather than hand-indexing `TXFM_TYPE_LS`/`COS_BIT_*` — a tightening over the 8x8/16x16 hooks, and worth copying (`txfm_type_col` is indexed by HEIGHT, `txfm_type_row` by WIDTH). **THE SEQUENCE: 8x8 fwd −2.017 % (25.74 %), 8x8 inv −1.538 % (26.96 %), 4x8+8x4 fwd −0.804 % (16.73 %), 16x16 fwd −0.600 % (5.62 %), 16x16 inv −0.480 % (5.04 %) — effect tracks CALL SHARE closely across a 5x span, which makes the remaining sizes predictable rather than speculative.** **THE 4x8/8x4 INVERSE IS LANDED — KB-PERF-28, `benchmarks/encoder_fused_inv_txfm_rect48_2026-09-09.md`: −0.310 %** (−0.254 % at 22/30 p=0.0161 and −0.366 % at 24/30 p=0.0014; null NOT significant, +0.165 % p=0.58), byte-identical, gate 1506/1506, green first run. **IT IS THE FIRST OF THE SIX FUSION KERNELS TO MISS ITS SHARE PREDICTION** — the inverse-8x8 rate would have predicted ~−0.87 % and it delivered a third. **Cause, measured not guessed: the rect kernel carries 2.5x the scalar glue of the square ones — five `from_array`/`to_array` sites against `inv_8x8_fused`'s two** — because at `row_n == 4` a load covers four of eight lanes, so vectors are built per-lane instead of with `from_slice`, and the reconstruction must LOAD the destination, add, clamp and store back per lane. The FORWARD twin has the same padding but hit its prediction, consistent with its store being one `copy_from_slice`. **So it is a fixable shortfall, not a ceiling** — branch on shape so the 8x4 reconstruction uses whole-vector `from_slice`/`store` with a 4-lane tail and the 4x8 row load uses `from_slice`; not done here because the effect is already positive and significant and the change deserves its own band. `INV_SHIFT` is `[0,-4]` for both, so there is **NO row shift at all**; the `NEW_INV_SQRT2` scaling lands BEFORE the clamp (that is the bite proof); and **`lr_flip` is a LANE reverse here, not KB-PERF-27's array-order reversal — the asymmetry is real** (the forward permutes column POSITIONS, the inverse permutes across LANES of one vector). **8x16 + 16x8 FORWARD IS LANDED — KB-PERF-29, `benchmarks/encoder_fused_txfm_rect816_2026-09-09.md`: −0.534 %** (−0.510 % at 27/30 and −0.559 % at 26/30, both p<=0.0001; null CLEAN at +0.045 %, p=0.58), byte-identical, gate 1506/1506, green first run. **IT WAS BUILT AS THE CONTROL FOR KB-PERF-28's DIAGNOSIS AND CONFIRMS IT**: rectangular 2:1 like 4x8/8x4 but with BOTH dims >= 8, so no padding — and its **0.059 percentage points per share-point lands squarely back in the unpadded band (0.048-0.107) against the padded inverse's 0.020.** So the padding/glue explanation holds and the KB-PERF-28 fix is worth doing rather than speculative. Written over GROUPS (`CG = col_n/8`, `RG = row_n/8`) so one body covers both shapes and matches the landed 8x8 (1,1) and 16x16 (2,2) structure; the transpose is CG*RG 8x8 blocks each landing on the (column group, row group) pair the row pass consumes. `FWD_SHIFT = [2,-2,0]` (16x16's recipe) with `rect_type = ±1`, and `lr_flip` uses KB-PERF-27's array-order reversal. Config from `get_fwd_txfm_cfg`, not hand-indexed tables. **THE 8x16/16x8 INVERSE IS LANDED TOO — KB-PERF-30, `benchmarks/encoder_fused_inv_txfm_rect816_2026-09-09.md`: −0.54 %** (25/30, p=0.0003) with a null that is EXACTLY flat (+0.00 %, 15/30, p=1.0000), so unlike the previous four bands the headline needs no two-copy averaging. **Its 0.057 percentage points per share-point over 9.52 % of inverses is the inverse-8x8 rate (0.057) to three decimals**, so the unpadded band is now 0.048-0.107 across SEVEN kernels and BOTH directions against the padded inverse's 0.020 — the padding explanation is confirmed from the inverse side as well as the forward, and **the KB-PERF-28 fix is the largest remaining item in this sequence.** Same group-parameterised body as the forward (`CG = col_n/8`, `RG = row_n/8`); `INV_SHIFT = [-1,-4]` (8x8's, not 4x8/8x4's `[0,-4]` and not 16x16's `[-2,-4]` — read from the table, not carried over); `rect_type = ±1` so the row pass carries `NEW_INV_SQRT2` BEFORE the clamp; `lr_flip` is the LANE reversal, which at `CG == 2` makes the two column groups exchange as well as reverse. Byte-identical on four cells, aom-dsp 446/446 in both dispatch modes green first run, gate 1506/1506 twice. Bite proof: dropping the column-group exchange fails both inverse differentials against the real exported C while 442 stay green. **THE KB-PERF-28 FIX IS LANDED — KB-PERF-31, `benchmarks/encoder_inv_rect48_unpad_2026-09-09.md`: −0.34 %** (mean of two base copies, −0.217 % at 22/30 p=0.0161 and −0.462 % at 25/30 p=0.0003; null +0.07 %, p=0.58, the other way). It branches on shape, because **each shape has one dimension that is a whole vector**: at 4x8 the row-pass coefficient load becomes `from_slice`, at 8x4 the `lr_flip` reversal becomes `revv` and the reconstruction becomes the whole-vector form; the 4-wide reconstruction becomes a fixed-size `[u16;4]` tail read rather than a slice walk. **It takes the padded 4x8/8x4 inverse from 0.020 to 0.043 percentage points per share-point against an unpadded floor of 0.048 — roughly four fifths of the gap — and does NOT close it.** State that plainly: the residual is the one thing branching cannot fix, that **in each shape one of the two passes runs eight lanes with four live**, which is inherent to a 4-dimension and is corroborated from the other side by KB-PERF-27 (the FORWARD twin, same half-idle passes, 0.048 — the lowest of the unpadded band). So the padded pair has a real ceiling just under the unpadded band, the glue explanation carried most of the distance to it, and **there is no further fix of this kind left here.** Byte-identical on four cells, aom-dsp 446/446 both modes, gate 1506/1506 twice. **THE RANKING WAS RE-TAKEN AT THE SHIPPING PRESET AND IT REORDERS — `benchmarks/encoder_x86_reprofile_1024_s3_2026-09-10.md`, the FIRST profile of this encoder at `--cpu-used 3` rather than 0** (port 3261.4 / C 1543.6 / **2.104x** / gap 1718 ms, 40,237 B both arms; from 2.287x at cycle start). §14's lesson applies to SPEED exactly as it did to platform and to frame size. Class table: transform +452 (26.4 %, ratio 2.78 — down from 4.26, which is the eight fusion kernels showing up), **intra-pred/rd +388 (22.6 %, ratio 2.93 — it was 8.1 % at s0, so this row MORE THAN DOUBLED and is now second)**, rd-driver +261 (15.2 %), txb/trellis +188 at **ratio 1.37** (still not a lever — `optimize_txb_core` 414 ms vs C's trellis 426, confirmed at BOTH speeds now), memory +132 (7.7 %, 6.65x), quantize +124 (7.2 %), **loop-restoration +114 (6.6 %, down from 14.1 % — libaom prunes Wiener/SGR hard below speed 5, so the stage KB-PERF-6/7/8 spent three landings on is a much smaller share of the preset that SHIPS)**, distortion +109 (6.3 %), postfilter +47. **Inside the new #2, like-for-like: `generate_hog` 36.2 vs 5.2 (7.0x), `cnn_predict` 34.9 vs ~2 (PARITY work, KB-41 root #27), `z3_high`+`z3_high_scalar` 40.7 vs 9.3 (4.4x — and 27.7 ms of that is GENUINELY SCALAR, i.e. KB-PERF-4's runtime gate is declining a quarter of z3 at this preset; whether that is the tap bound or the shape gate is UNMEASURED and the reach counters would answer it in one run), `highbd_filter_intra_edge` 27.7 vs 6.8 (4.1x), `cfl_predict_block` 17.0. But the EDGE ASSEMBLY is 43.3 vs C's 50.9 — 0.85x, FASTER — so do not spend a landing there; same shape as the `optimize_txb_core` finding one class over.** **FIRST LEVER OFF IT IS LANDED — KB-PERF-32, `benchmarks/encoder_hog_row_windows_2026-09-10.md`: −0.20 %** (mean of −0.169 % at 21/30 p=0.0428 and −0.227 % at 22/30 p=0.0161; null EXACTLY flat). `generate_hog` read eight taps per pixel through a closure over the whole plane, each a bounds check the compiler cannot hoist; it now walks three row slices as overlapping `windows(3)`, which is one bounds check per ROW instead of eight per PIXEL with the arithmetic, walk order and f32 accumulation order untouched (the HOG differentials against real exported C are what make that an assertion rather than a claim). **§14 AGAIN, and record the number: the kernel went 36.2 -> 25.3 ms (1.43x), the encode −6.5 ms — so the bounds checks were about a THIRD of the kernel, not the kernel.** 4.8x optimistic against the class row, 1.7x optimistic against the kernel's own self cost, which is the honest way to cost this kind of change and still not tight. What is left in it: an integer division per interior pixel (`(dy << 16) / dx`, not removable without an exactness argument about truncation toward zero) and u16 loads where libaom at bd8 reads u8. Still 4.9x and still the worst ratio in its class. **THE DIRECTIONAL-PREDICTOR GATE WAS CENSUSED AND A PUBLISHED REACH FIGURE IS CORRECTED — `benchmarks/encoder_dir_pred_reach_audit_2026-09-10.md`** (throwaway counters, reverted; nothing in the encoder changed). **THE RUNTIME TAP BOUND DECLINES NOTHING — 0 of 2,884,594 calls across z1, z2 and z3.** KB-PERF-4 derived `M* = 1023` and pays an `O(bw+bh)` scan per block to check it; on bd8 that scan is a pure cost with no decline behind it. It CANNOT be deleted — the gate is on the DATA not on `bd`, which is what keeps the kernel safe for a bd10 caller and for the public entry point — but its price is now known to be paid for nothing at the only depth the encoder's profile reaches. **The decline is entirely `up == 1`** (the upsampled edge, taps at stride 2): 54.9 % of z1 calls, 13.8 % of z2, **64.9 % of z3**, i.e. 38.2 % and 34.1 % of z1's and z3's predicted PIXELS. **KB-PERF-4's recorded reach is wrong at this preset, by ~2x and in BOTH directions**: z1 `up==0` recorded 87.4 % / measured **45.1 %**, z2 above-half 49.8 % / **86.2 %**, z3 `up==0` 85.1 % / **35.1 %**. Nothing about the kernel is falsified — those figures were true of that landing's own corpus — but they were being READ as a statement about the shipping preset. **§14 for REACH figures, not just cost figures: a reach number carries its regime too.** **THE LEVER IT NAMED WAS BUILT, MEASURED TWICE AND REJECTED, and record why.** At `up == 1` output `r` reads `edge[base+2r]` and its successor — i.e. element `2r` of the CONTIGUOUS two-tap run at twice the length — so running `2n-1` elements and keeping every second result is bit-exact by construction and needs NO de-interleave (and stays tier-agnostic, so NEON/wasm would get it too). Both variants were implemented, gated, bite-proved (selecting the ODD element fails `dr_predict_high_matches_c` and `highbd_dr_predictors_identical`, both against real exported C, while 442 stay green) and byte-identical on four cells — and **both are SLOWER: z1+z3 +0.21 % (10/30), z3 alone +0.34 % vs base and +0.28 % vs baseB at 6-7/29, p=0.0023, null dead flat.** REVERTED. **Mechanism, and it is the THIRD instance: `two_tap_run_impl` is STORE-bound, not arithmetic-bound.** It computes 16 lanes, stores once to `buf`, then copies out PER LANE; running `2n-1` to keep `n` leaves the vector op count unchanged at `bh == 8` (15 and 8 lanes are both one 16-lane vector) but DOUBLES the per-lane copy and adds a strided read on top. KB-PERF-15 found `txb_init_levels` spending its time in eight single-byte stores around already-correct SIMD, and KB-PERF-5 rejected PAETH's half on the same evidence. **Before doubling work to avoid a shuffle, check whether the kernel's cost is in the shuffle at all.** What WOULD pay is removing the per-lane copy — a whole-vector store into the destination — which for z3 means libaom's own transposing structure, a rewrite rather than a gate change. **A COPY AUDIT WAS RUN AND LANDED TWO THINGS — KB-PERF-34/35, `benchmarks/encoder_copy_sweep_2026-09-10.md`: −0.48 % together** (−0.492 % at 28/30 and −0.472 % at 26/30, p<=0.0001; null +0.058 %, p=0.36), split as **two_tap −0.22 %** (banded alone first) and **the chroma `to_vec()` ~−0.23 pp**. **(34) `two_tap_run` stored 16 lanes then copied out PER LANE with an `as u16` cast; it now bitcasts and does ONE `copy_from_slice`. This is the DIRECT CONVERSE of the rejection hours earlier**: doubling that kernel's arithmetic to avoid a gather measured +0.34 % and was diagnosed store-bound; removing the copy measures −0.22 %. Same kernel, same box, same day, opposite signs — **the rejection paid for the fix.** The bitcast is EXACT (outputs are non-negative and <= 1023, so the i16 bit pattern IS the u16 value — the equality the old cast relied on). **(35) `txfm_rd_in_plane_uv_p` still did `t_above/t_left = ...to_vec()` — two heap allocations per call in a 48.9 ms symbol. KB-PERF-13 removed exactly this from the LUMA walk and did not carry it across.** `MI_SIZE_WIDE_B`/`_HIGH_B` cap at 32, so `[i8; 32]` suffices. **~0.23 pp for deleting two allocations is the largest per-line return this cycle produced.** **WHAT THE AUDIT ALSO SAYS — the allocator class is effectively CLOSED.** Measured with a frame-pointer profile (KB-PERF-15's instrument), `malloc` is **0.25 % of the profile** and its only named callers were the two functions above (the second's residual is the `winners` Vec it RETURNS). `__memmove` 2.04 % and `__memset` 1.34 % remain, but their callers are legitimate buffer fills inside transform/txb kernels (`av1_fwd_txfm2d_into`, `optimize_txb_core`, `txb_init_levels`, `assemble_dir_edges`) — NOT borrow-checker artifacts, and KB-PERF-2 already built and rejected skipping the scratch re-zero because that zero-fill is what makes scratch reuse byte-identical BY CONSTRUCTION. A third site (`encode_intra.rs`, twice) has the same shape but its arrays are RETURNED in `EncodeIntraPlaneOutcome`, so removing the allocation needs a struct change and its consumers — out of proportion to a 0.25 % class, attempted and reverted, named in the record. **PROCESS FAILURE WORTH KEEPING: that third site NEVER COMPILED and three builds silently shipped a STALE binary, because `cargo build | grep -E "^error"` matches nothing — cargo's error lines start with ANSI colour codes** (this repo's own `strip-ansi-before-grepping-cargo` memory documents it, and it was still hit). It was caught only by a sha256 comparison coming out IDENTICAL across three different source states, which is impossible for real codegen. **Compare binary hashes between arms before trusting a band, and pipe cargo through `sed -r 's/\x1b\[[0-9;]*m//g'` before any anchored grep.** The consequence was benign here (one band re-measured a comparison already made, and agreed), but a stale arm in an A/B is a silent wrong answer. **THE `quantize` CLASS — the one the 1 MP table calls "never examined, and the one class that GREW" — IS NOW EXAMINED. KB-PERF-36, `benchmarks/encoder_quantize_fp_tune_2026-09-10.md`: −0.18 %** pooled over TWO independent 30-round bands (−0.185 % at 42/60 p=0.0027 and −0.170 % at 45/60 p=0.0001; pooled null not significant). Pooled because band 1 came back marginal (one comparison at p=0.043, the other at p=0.36) — and the pooled median sits BETWEEN the two band medians, so no favourable band was selected. At s3 the class is +124 ms / 7.2 % / ratio 3.27, and it splits into two very different problems: **`quantize_fp_impl_v3` 78.1 ms vs `av1_quantize_fp_avx2`+`_32x32` 25.1 (3.1x)** and **`aom_quantize_b_no_qmatrix` 26.9 ms vs `aom_quantize_b_avx2`+facade 3.6 (7.5x)**. Fixed in the first: the eob path built its vector PER LANE every chunk (8 bounds-checked `i16` loads → one fixed-size `[i16; 8]` read) — **the same defect KB-PERF-34 and KB-PERF-35 each measured at ~0.2 pp, now in a THIRD kernel** — and four `splat`s were rebuilt per chunk although only chunk 0 carries the DC lane, so they are loop-invariant from chunk 1 (4 x 127 splats removed on a 32x32). Neither changes a value or a lane. **NOT DONE, DELIBERATELY, and this is the biggest single un-taken kernel left: `aom_quantize_b_no_qmatrix` has NO SIMD AT ALL (7.5x, ~+23 ms).** Its loop walks SCAN order and writes at scattered raster positions, so vectorizing means processing in RAW order. That is provably value-identical for the COEFFICIENTS (the pre-scan's dead-zone test is the same test the main loop applies, so skipped positions quantize to zero and are already zero from the opening `fill(0)`) — **but it changes how `eob` is derived, and `eob` is exactly the quantity KB-12 documents as the single ORDER-SENSITIVE output of this family** (rate, distortion and skippability are order-invariant sums; a dropped transpose there read as a genuine RD near-tie across four localization passes). Tractable and worth ~23 ms, but it needs its own landing with an explicit eob argument and a bite proof AIMED AT THE EOB — not a rushed edit. **THE RANKED LEVER FOR CLAUSE (4) IS NOT THE PLANE REFACTOR — IT IS THAT THE FUSION SEQUENCE BYPASSED THE i16 PATH. `benchmarks/encoder_clause4_narrowpath_2026-09-10.md`.** Measured at the shipping preset INSTEAD of starting the u16 refactor, because the figure that refactor is sized by (~42 % of the gap) comes from a **192x192 `--cpu-used 0`** entry and this session had already corrected two other stale regime-carrying figures. **Only 9.1 % of the port's 1-D transform work runs on the i16 path** — `run_fwd1d_v3` + `run_inv1d_v3` are **285.9 ms on the WIDE i32 path** against **28.5 ms** narrow, and libaom's narrow equivalents (`fdct8x8_new_sse2`, `fadst8x8_new_sse2`, `av1_idct8_sse2`, `av1_iadst8_sse2`) total **45.0 ms — so the wide path is 6.4x libaom's narrow one.** **WHY: KB-PERF-3's i16 kernels are reached only from the GENERIC driver's passes (`try_fwd_col_pass`/`try_fwd_row_pass`/`try_inv_row_pass`), and every fused whole-transform kernel (KB-PERF-23..30) calls `run_fwd1d`/`run_inv1d` — the i32x8 kernels — DIRECTLY, with the driver hooks routing around the generic passes. The fused hooks now cover ~97 % of transforms by call share, so KB-PERF-3's −2.0..−2.5 % landing is bypassed for almost everything.** **NOTHING COULD HAVE CAUGHT IT**: every fusion landing was byte-identical so no gate moved, and every one was net NEGATIVE on the wall clock because the fusion win exceeded the i16 loss it was silently taking. **A landing can be a real win and still give back a larger win that already existed — check whether a new fast path BYPASSES an older one before crediting its band.** Corroboration that the narrow path is where libaom's speed comes from: classifying every C symbol, **268.3 ms of libaom's 1550.7 ms encode (17.3 %) is on narrow-path kernels.** **THE LEVER: restore i16 INSIDE the fused kernels** — **AND IT IS NOW SCOPED EXACTLY, 2026-09-10, so the next session does not re-derive it. THE STRANDED SET IS TWO KERNEL FAMILIES, NOT EIGHT.** Enumerated from the dispatcher rather than assumed: the fused forward hooks are `fwd_8x8_fused`, `fwd_rect48_fused` (4x8/8x4), `fwd_rect816_fused` (8x16/16x8) and `fwd_16x16_fused`, plus their inverse twins. **8x8 and 4x8/8x4 have NO dimension >= 16, so i16 buys them NOTHING** (KB-PERF-3's half-idle null: an `i16x16` and an `i32x8` are both 256 bits). **And 4x16/16x4 are NOT fused at all**, so they still route through the generic driver and ALREADY get i16. So the recoverable set is exactly **`{fwd,inv}_16x16_fused` + `{fwd,inv}_rect816_fused`** = 5.62 % + 9.02 % = **14.6 % of forwards**, which is what the 22.2 %-of-1-D-work / ~1 % figure above is made of. **The infrastructure is already built and was NOT rebuilt here: `transform/simd/lowbd16_fwd.rs` has `run_fwd1d_i16`, the per-kernel bound `fwd_i16_max_in` (fdct16 2895, fadst16 3214, fdct8 5791, fadst8 6419) and the runtime gates `fwd_col_i16_applies`/`fwd_row_i16_applies`.** **THE 'BLOCKER' IS MUCH WEAKER THAN FIRST RECORDED, AND THE FIRST STATEMENT OF IT IS CORRECTED HERE THE SAME DAY — a wrong reason closes a row for everyone after you (KB-PERF-53 vs KB-PERF-47 is this file's own precedent). The original claim was 'holding 16 lanes needs a 16-lane i16 TRANSPOSE and magetypes 0.9.29 has no integer interleave/unpack'. The second half is true OF MAGETYPES and irrelevant, because THE FUSED KERNELS DO NOT USE MAGETYPES FOR THIS: `fwd_16x16_fused`'s own `tr8` closure is written in RAW AVX2 — `_mm256_unpacklo_epi32`, `_mm256_unpackhi_epi64`, `_mm256_permute2x128_si256` — inside `aom-dsp` with `#![forbid(unsafe_code)]` in force. **An i16 transpose is the SAME technique one instruction over (`_mm256_unpacklo_epi16`), not a new capability**, and the port ALREADY has that instruction wrapped as `prims16::unpk16`. (Caveat, because it is a trap: `unpk16` is NOT portable — on NEON it is a no-op passthrough, since `vmull_n_s16` pairs differently — so it is a madd-pairing helper, not a transpose primitive, and an i16 transpose would be x86-only like hadamard and wiener.) **So the FULL step (both passes i16, ~50 % of the 1-D ops on those two families) is available, not blocked** (`docs/MAGETYPES_VOCABULARY.md`; the transpose primitives are f32-only). **The precedent for doing it anyway is KB-PERF-21/22: raw AVX2 intrinsics COMPILE inside `aom-dsp` with `#![forbid(unsafe_code)]` in force** (a `#[rite(v3)]` body carries `target_feature(avx2)`, and `archmage::intrinsics::x86_64` re-exports `core::arch`), so `_mm256_unpacklo_epi16` is reachable — at the cost of being x86-64 only, with aarch64/wasm keeping the i32 path, exactly as hadamard and wiener do. **AND THE HALF-STEP'S ONE RISK IS ALREADY RETIRED:** the lane ordering of `prims16::widen_lo`/`widen_hi` (i16x16 -> two natural-order i32x8) is the index-slip class that was the ONLY defect in KB-PERF-23 and KB-PERF-24 — but `fwd_col_pass_i16` (`lowbd16_fwd.rs:377`) already writes a 16-wide i32 buffer with exactly that widen_lo/widen_hi pair in production, under the existing i16 differentials. Reuse it rather than re-deriving it. **A CHEAPER HALF-STEP EXISTS AND IS UNMEASURED: run PASS 1 in i16, widen to i32 before the EXISTING 8x8-block transpose, keep pass 2 wide.** That needs no new transpose and no intrinsics, and halves one pass of two — so ~25 % of the transform's vector ops rather than ~50 %. **THE HALF-STEP WAS THEN BUILT AND MEASURED NULL — 2026-09-10, `benchmarks/encoder_i16_fused_col16_2026-09-10.rejected.md`.** i16 lanes in `fwd_16x16_fused`'s COLUMN pass: correct (byte-identical on all four pinned cells, 28/28 transform differentials incl. `txfm2d_differential_fuzz` vs the real exported C) and genuinely REACHED (perturbing only the new branch moves the shipping cell to 41,045 B and fails exactly 3 tests while 25 stay green). **Measured +0.185 % at 7/30 against a same-binary null of +0.150 % — lever minus null ~+0.035 pp, inside the ~0.27 pp copy systematic. Null to slightly slower. REVERTED.** **THE DURABLE RESULT IS WHY: 16x16 is 5.62 % of forwards and this changed ONE of its two passes, so the reachable share is ~0.12 % of the encode — BELOW this box's ~0.2 % band resolution. It was never measurable alone**, and against that ceiling it ADDS work (`fwd_col_i16_applies` scans all 256 samples per transform; 32 `vpmovsxwd` to widen). **So the ~1 % figure is for the FULL step across BOTH families and BOTH passes; the per-family, per-pass slice is ~0.12 %, unmeasurable, and pays the bound scan separately. A LEVER THAT ONLY EXISTS IN AGGREGATE CANNOT BE LANDED OR VALIDATED IN INCREMENTS — build all four kernels and band it as ONE change, or not at all.** What that experiment DID retire, for whoever takes the full step: the magetypes/token plumbing compiles (cross-module `incant!` into `lowbd16_fwd`'s i16 runner works, 3 trivial path-qualification errors and nothing else), the widen ordering is proven, and the bite recipe is recorded. **Do not start either without a band; this file's §14 record is 18x, 13x and 5x optimistic on exactly this kind of arithmetic.** NOT ATTEMPTED here deliberately: it is bit-exactness-critical work on the transform, and a half-finished, un-banded transform landing is worse than none. — not a refactor, a bound check plus a kernel selection in code written this cycle; the fused structure is unchanged, the intermediates are held as `i16x16` where the runtime bound admits. **CEILING CORRECTED THE SAME DAY, BEFORE ANYONE SPENT A SESSION ON IT: it is ~1.96 %, NOT 4.2 %, and this repo's own measurement is what refutes the bigger number.** KB-PERF-3 BUILT the half-idle variant (8-dimension blocks as 16-lane i16 batches) and **measured it NULL** (−0.009 % vs a +0.08 % null) — an `i16x16` and an `i32x8` are both 256 bits, so a pass whose vectorized dimension is under 16 gains NOTHING. Halving assumed every pass fills; **82.98 % of forward transforms have BOTH dimensions < 16**, and most of the rest fill only ONE of their two passes. Weighting each pass by real vector work (`ceil(dim/lanes)` groups of an `n log n` kernel, over the s3 census) the recoverable share is **22.2 % of 1-D transform work = 63 ms = 1.96 % of the encode**, and with this session's measured **1.7x-4.8x** optimism that is **0.41 %-1.15 %.** So it is a ~1 % item. **Start with the FORWARD half** (KB-PERF-3 measured it −2.0..−2.5 %); the **INVERSE i16 was measured NULL** as a standalone (−0.03 %, 5/8) — that null was on the GENERIC driver where intermediates round-trip through `buf`, and inside a fused kernel they stay in registers, so it is a DIFFERENT comparison, but close enough that **the inverse half must get its own band before being assumed.** **The u16 plane refactor is DEMOTED, not dismissed**: its lane-width half is landed (forward) or measured null (inverse) and its size-specialisation half is ~97 % done by the fusion sequence; what remains uniquely to it is u8 loads/stores, which is the RESIDUE after this lever and needs its own measurement rather than the 192x192 figure. **This lever does NOT close clause (4) either** (the bar needs −915 ms, 54 % of the gap), and after the correction it is a ~1 % item, so it does not change the SHAPE of the problem. **ON THE MEASURED EVIDENCE THERE IS NO REMAINING LEVER ABOVE ~1 % — BUT THE SECOND HALF OF THAT SENTENCE, AS ORIGINALLY WRITTEN ("and 1.5x is not reachable by kernel work"), IS WRONG AND IS CORRECTED IN `benchmarks/encoder_clause4_reachability_2026-09-10.md`.** The bar needs **−915 ms of a 1814 ms classified gap = 50 % of EVERY class gap**, not 100 % of any one — and **halving every class gap gives 2334 ms = 1.505x, the bar exactly.** That is not a hypothetical rate: **the transform class went 4.26x -> 2.78x THIS CYCLE, a 45 % gap reduction**, via eight fused kernels of 0.3-2.0 % each — i.e. by a programme of exactly the sub-1 % landings the first half of the sentence dismisses. Both halves of the original claim were individually true; inferring unreachability from "no big lever" was the error. **REDUNDANT WORK (the KB-PERF-1 shape) IS STRUCTURALLY RULED OUT and the programme should not chase it**: the port is byte-identical to libaom on this cell and 427 gated cells with ported speed features, so the same RD decisions ⇒ the same candidates ⇒ the same kernel invocation counts; and the profile corroborates — the DRIVERS run at 1.29x (`search_tx_type_intra_into` 143 ms vs `search_tx_type` 111) and 1.37x (txb) while the KERNELS run 2.8x-6.7x, whereas redundant calls would inflate the drivers too. **So the whole 1690 ms is PER-OPERATION cost** — a kernel-efficiency problem with a known end state, not a structural one. Ranked remaining classes: transform +453, intra-pred/rd +388 (barely touched), rd-driver +261 (never drilled at s3 — `txfm_rd_in_plane_intra` 124 ms vs ~44 C, 2.8x), memory +132 at 6.74x, quantize +124, LR +113, distortion +108, postfilter +46 (the last two never examined at any preset). **Leave txb/trellis alone at 1.37x.** The honest recommendation is therefore NOT "ship or reconsider the bar" but **"the bar is reachable; it is roughly eight more cycles of what this cycle did to the transform class"** — a funding decision, not a technical one. Take the lever because it is the largest single item left and it recovers a win the port already had — not as a step toward the bar. **KB-PERF-37 — `highbd_subtract_block` DID THREE BOUNDS CHECKS PER PIXEL: −3.17 %, `benchmarks/encoder_subtract_block_2026-09-10.md`** (−3.170 % and −3.188 % vs the two base copies, **30/30 rounds faster on BOTH**, p<0.0001; null dead flat). **Ten times the largest landing of this cycle, from six lines** — and it changes the ranking of INSTRUMENTS, which is the durable part. **A CLASS TABLE CANNOT SEE A HOT LOOP INLINED INTO A LARGE CALLER**: the nine landings before it came from class tables and symbol shares and averaged 0.3 pp; this came from `perf annotate` on ONE symbol. `rd-driver` read as +261 ms of diffuse driver overhead at ratio 2.19; annotating its top symbol (`txfm_rd_in_plane_intra`, 124 ms vs ~44 C) put **~20 % of it in a single basic block** — a `movzwl`/`subw`/`movw` chain with THREE `cmp` bounds checks around it and no vectorization, i.e. `highbd_subtract_block` inlined, against libaom's `aom_subtract_block_sse2` at 10.9 ms. **The defect**: three independent runtime slice lengths indexed by `base + c`, so the compiler emits a bounds check per buffer per element AND cannot prove the accesses contiguous, blocking vectorization of a plain 16-lane job. **The fix**: one row slice per buffer per row, zipped — one bounds check per ROW instead of per ELEMENT. **Bit-exact by construction**: `(a as i32 - b as i32) as i16` truncates to 16 bits, which IS the 16-bit wrapping difference, and the slice bounds are the ones the indexed form already required. **KB-PERF-38, THE FOLLOW-UP: ONE FIX WAS NOT ENOUGH, AND RE-ANNOTATING IS WHY WE KNOW. −0.23 %** on top (−0.228 % at 21/29 p=0.0241 and −0.225 % at 26/29 p<0.0001; null flat). Re-profiling moved `highbd_subtract_block` OUT of its callers into its own symbol at **54 ms — still 5x libaom's 10.9**. The second annotate showed the cost was now **the ROW loop, not the inner one**: two `imul`s and three bounds checks PER ROW around a `movq` of **four** i16 lanes — and **TX_4X4 is 40 % of transforms**, so a typical row is four columns and the per-row overhead exceeds the work it guards. **The rewrite had also COST ITS OWN INLINING**: before KB-PERF-37 the body was inlined and LLVM specialised it on the callers' constant `cols`/strides; as a standalone symbol that was gone. Fixed with running offsets (`off += stride` — same values, same slice bounds) plus `#[inline]`. **TRANSFERABLE: a fix that turns a hot INLINED loop into a standalone symbol can lose more to lost specialisation than it gains — RE-ANNOTATE after landing, not just re-band. The band said −3.17 % and looked finished.** **KB-PERF-47 — THE SAME TWO FIXES ON `variance_raw` MEASURED NULL (+0.245 %/+0.101 %, p=0.31/0.54; null +0.070 %). REVERTED — and it REFINES the `#[autoversion]` rule.** `dist::variance` is 35 ms, scalar (no `__arcane_` prefix) where its highbd twin is vectorized, and `variance_raw` has the identical two-bounds-checks-per-element + serial-accumulator shape. **`#[autoversion]` adds a RUNTIME DISPATCH PER CALL.** It paid in `block_error` (−0.88 %) and `sum_squares_2d_i16` (−0.35 %) because those run over whole TRANSFORM BLOCKS — few calls, large per-call work. `variance` is called on small blocks from inter/motion paths (`compound_type`, `rdopt_sse`, `firstpass`, `inter_me`) largely dead on an ALLINTRA KEY frame, so the dispatch never amortizes. **`#[autoversion]` pays when per-call work is large enough to amortize a dispatch — check the CALL SIZE, not just the symbol's total milliseconds.** **KB-PERF-46 — THE NAMED SIBLING, TAKEN: `sum_squares_2d_i16` −0.35 %** (−0.338 % at 23/24 and −0.360 % at 21/24, p<=0.0003; null −0.080 %, p=0.15), byte-identical. It is LIVE inside `search_tx_type_intra_into` — annotating that symbol shows its `movswl`/`imull` chain with **two `cmp` bounds checks** around it, the same pattern KB-PERF-37 fixed one file over. Two already-proven fixes in one: **one bounds check per ELEMENT** (runtime slice length indexed by `base + c`) -> a row slice, one per ROW; and **a serial `u64` accumulator blocking vectorization** -> `#[autoversion]`, the attribute that gave KB-PERF-39 its −0.88 %. Bit-exact: `v * v` stays i32 (non-negative, <= 2^30 for an i16 input, so `as u64` is exact) and vectorizing REASSOCIATES the sum, which is exact because u64 addition is associative and the total cannot overflow (2^30 over <= 128x128 is under 2^44). **Both siblings that record named are now resolved** — this one landed, and the lowbd `subtract_block` twin is DECODER-path only (the encoder is u16-through at every bit depth) so it never appears in the encoder profile. Historical: **SIBLINGS OF THE SAME SHAPE, NAMED AND NOT YET MEASURED: `subtract_block` (the lowbd u8 twin, same file — not in the encoder profile because the encoder is u16-through, but it is on the DECODER path) and `sum_squares_2d_i16` (one bounds check per element plus a serial `u64` accumulator blocking a trivially reducible sum). Both free to fix by the same method. AND THE GENERAL LESSON: `perf annotate` the top symbol of any class that reads as "diffuse driver overhead" before believing that description.** **KB-PERF-39 — `block_error` VIA `#[autoversion]`: −0.88 %, `benchmarks/encoder_block_error_autoversion_2026-09-10.md`** (−0.887 % and −0.864 % vs the two base copies, 25/28 each, p<0.0001; null +0.058 %, p=0.34). **The THIRD landing found by ANNOTATING a symbol rather than reading a class table, and the cheapest: no new kernel, no intrinsics, no `unsafe`.** `distortion` was an unremarkable +108 ms / 6.3 % row never examined at any preset; annotating its symbol `dist_block_tx_domain` (61 ms vs libaom's `av1_block_error_avx2` 8.4 = **7.3x**) showed a loop already bounds-check-free and simply SCALAR — 32-bit `imull`, `cltq`/`movslq` sign-extend, 64-bit add, two-way unrolled — where libaom does the same arithmetic 8 lanes wide. **`#[autoversion]` (archmage) alone fixes it**: it compiles the body once per SIMD tier with the target features enabled, which is all LLVM's auto-vectorizer needs to emit 8-lane `pmulld` with widening accumulation. The crate ALREADY had this pattern (`sad_simd`, same file) — a tool sitting unused. **Bit-exact**: per-element arithmetic untouched (`wrapping_sub`/`wrapping_mul` in i32 — the wrap is load-bearing, KB-ARM-FLOAT root #3 — then sign-extended to i64), and vectorizing REASSOCIATES the two sums, which is EXACT here because i64 addition is associative and neither accumulator can overflow (|diff*diff| <= 2^31 over <= 4096 coefficients is under 2^43). The verbatim scalar loop is kept as `block_error_scalar_ref`. `highbd_block_error` (bd10/12) is untouched — i64 products vectorize far less well. **KB-PERF-43 — THE TEMPORARIES PROBLEM IS LARGELY SOLVED: −78 % of TEMPORARY allocations, −23 % of ALL allocations, −0.457 % wall. `benchmarks/encoder_smallvec_winners_2026-09-10.md`.** 10,419,121 -> **8,010,969** allocations and 1,931,251 -> **417,969** temporaries per 1 MP encode (21/24 rounds, p=0.0003; null −0.023 %). The fix: `txfm_rd_in_plane_intra`'s returned `winners` becomes `pub type TxbWinners = smallvec::SmallVec<[TxbWinner; 4]>`. **The value ESCAPES four layers with keep-best semantics, so a scratch would need a two-buffer swap through all four — an inline small vector is a DROP-IN for the return type instead.** Inline capacity 4 chosen from MEASUREMENT, not guessed: a throwaway histogram gives **1 txb 50.6 %, 2 txbs 35.6 %, 4 txbs 13.8 %, NOTHING above 4**, and the 1,359,984 call count matches heaptrack's allocation count for that function EXACTLY — which is what proves `winners` was 100 % of it. **`smallvec` BEATS `tinyvec` BY HALF A PERCENT OF THE WHOLE ENCODE AT IDENTICAL ALLOCATION COUNTS — measured, and it overrode a sound-looking argument.** The first implementation used `tinyvec` because it is itself `#![forbid(unsafe_code)]` and matches workspace policy. Four-arm rotated band, same tree, same counts (8,010,969 / 417,969 both): **tinyvec −0.076 % (14/24, p=0.54, NOT significant), smallvec −0.457 % (21/24, p=0.0003), smallvec vs tinyvec −0.500 % (20/24, p=0.0015).** Mechanism is structural: **`TinyVec` is an ENUM (`Inline | Heap`) so every push/index/iteration branches on the discriminant; `SmallVec` uses a capacity field + union and the inline path does not.** On a container touched millions of times per encode that branch IS the difference. `smallvec`'s `unsafe` is inside the DEPENDENCY, and `forbid(unsafe_code)` is per-crate, so no policy is weakened. **It also corrected its own first number** — an earlier 3-arm band had put tinyvec at −0.26 % (p=0.0014); the 4-arm rotated band says −0.076 % and not significant. **KB-PERF-44, the NEXT site, found by obeying the frame-pointer rule: `partition_pick::perpixel_variance_y` was `let offs = vec![128u16 << (bd - 8); w];` — a heap allocation of a CONSTANT-valued buffer (<= 128 entries), 220,000 times per encode, 100 % temporary.** Replaced by a `[u16; 128]` stack array + a `w`-element fill (`highbd_variance` reads it with stride 0, so only `offs[..w]` is touched). **Allocations 8,010,969 -> 7,790,969 (−220,000, exactly the predicted count); TEMPORARIES 417,969 -> 197,969 (−53 %). CUMULATIVE across KB-PERF-43+44: temporaries 1,931,251 -> 197,969, a 90 % REDUCTION.** Wall −0.074 %/−0.065 %, **NOT significant on glibc** (15/24, p=0.31) — **and it is KEPT anyway, where the capacity hint was REVERTED, which is the distinction worth remembering: the capacity hint ADDED arithmetic (two divisions per call) to save allocations that did not exist and measured +0.77 %; this STRICTLY REMOVES work (no malloc, no free, and the fill is the same `w` stores `vec![v; w]` already did), so it cannot be slower in principle and it measures on the right side of zero, just under what 24 rounds resolve** (220k malloc/free pairs at ~30 cycles ~ 1.5 ms = 0.05 %, matching the observed −0.07 %). **THE libaom BASELINE, WHICH NOBODY HAD MEASURED: heaptrack on the C arm of the same 1 MP cell gives 1,055,245 allocations / 7,209 temporary.** The port started this pass at 10,419,121 / 1,931,251 — **9.9x and 268x** — and is now at **5,106,723 / 336,064 = 4.8x and 47x**. libaom is NOT at zero, so a floor near 1 M is what "as good as C" means here. **KB-PERF-50 — WHERE THE REMAINING MILLIONS WERE, and it is a one-line defect: `intra_model_rd_y` sat on top of 3,527,376 of 6,922,557 allocations and NOTHING INSIDE IT ALLOCATES** (it only does `clear()`+`resize()` on a correctly-threaded scratch). The cause was `intra_rd.rs:1103`, `let mut txs = IntraTxScratch::default();` — **`rd_pick_intra_sby_mode_y` built a FRESH scratch on EVERY call, and it is called once per leaf.** Its own comment says the scratch exists so "every mode x tx size x txb x candidate tx type shares it" — and they do, WITHIN a call; it was thrown away BETWEEN calls, so every buffer started empty and regrew. The chroma twin `rd_pick_intra_sbuv_mode` had the same line. Both pooled (KB-PERF-48's pattern; neither has an early return, so take-at-entry/restore-at-exit is total). **Allocations 7,378,761 -> 6,922,557 (KB-PERF-49, `SmallVec<[i32; 32]>` coefficients) -> 5,106,723 = −2,272,038, −31 %; wall −0.151 %/−0.068 % (not significant, null −0.050 %); byte-identical on four cells.** **The pool PAID FOR the coefficient change** — KB-PERF-49 alone measured +0.46 %, and together they are net negative. **THE GATE EARNED ITS KEEP: the `SmallVec` field type broke NINE differential assertions comparing a txb's coefficients against the C oracle's `Vec`, and `-p zenav1-aom-encode` passed 772/772 while the INTEGRATION targets did not compile — KB-42's rule exactly.** Fixed by comparing as slices, preserving each assertion's meaning. **KB-PERF-50 cont. — THE SAME DEFECT AT THE TWO PALETTE SITES, and the chroma one has a trap.** `rd_pick_palette_intra_sby` and `rd_pick_palette_intra_sbuv` each built their own `IntraTxScratch::default()` per call, one call per leaf reaching the palette search. Both pooled — **but the chroma pool is taken AFTER its early return, not at entry**: `rd_pick_palette_intra_sbuv` returns early when the colour count misses the threshold, so taking at entry would hand the pool back EMPTY on every early exit, i.e. reproduce the defect on the majority path. Luma and chroma get SEPARATE statics because the chroma search runs nested inside `rd_pick_intra_sbuv_mode`, which already holds its own pool. **Reach is stated rather than assumed: palette is knob- AND header-gated (`--enable-palette` + `allow_screen_content_tools`), so on the photographic cells every other allocation number is measured on, these two functions are NEVER called and the change is inert by construction.** Measured where it does fire (`winperf::SCREEN_GATE_CELL` 512x384 cq44 s6, `Content::Screen`, palette on): **251,496 -> 241,140 allocations (−10,356, −4.1 %)**, 154.0 -> 150.6 MB, coded length **6,131 B identical**. No wall band: a 4 % cut on a path the default still-image encode does not take would be a statement about screenshots, and the palette byte gates carry the correctness claim. **A DOCUMENTATION DEFECT THE POOL PATTERN CREATES, worth knowing before adding the next one: inserting `thread_local! { .. }` immediately above a function puts it BETWEEN that function's doc comment and the function**, orphaning the docs onto the macro — `rustc` says `unused doc comment`, and three sites had it (both pools above plus KB-PERF-48's `encode_intra.rs`). At `intra_uv_rd.rs` it was worse: the block landed between `#[allow(clippy::too_many_arguments)]` and the function, so the ATTRIBUTE was orphaned too and clippy would have begun flagging it. **Put the `thread_local!` block ABOVE the doc run, not adjacent to the function.** **THE WHOLE ALLOCATION PASS IS NOW SIZED ON WINDOWS, AND THE PLATFORM RATIO IS MEASURED ON IDENTICAL WORK — `benchmarks/encoder_alloc_pass_windows_2026-09-10.md`, run 34498748007 (`arms: prepost`, `7062a06` -> HEAD, 24 rounds) plus a MATCHED Linux band built locally from the same two commits with the same harness.** Six bands, all negative, all significant: **linux x86-64 −0.350 % `detail` / −0.712 % `photo`; `windows-11-arm` −1.433 % / −1.188 % (24/24 both, p<0.0001); `windows-latest` x86-64 −2.432 % / −2.249 %.** **The same source change is worth 6.9x more on Windows x86-64 than on Linux x86-64 at `detail`, 3.2x at `photo`.** **What makes this the SHARP form of the claim KB-PERF-2 could only infer: the allocator censuses are IDENTICAL TO THE DIGIT on the two x86-64 platforms** — 408,878 -> 300,515 calls and 261,032,010 -> 183,750,250 bytes at `detail`, 317,426 -> 239,630 at `photo`, same buckets — so nothing about the AMOUNT of work differs and the entire 6.9x is **cost per allocator call**. (The aarch64 runner differs because a different SIMD tier is dispatched, changing scratch shapes; reported separately, never merged.) `framebytes` is 8,734 / 5,301 on EVERY arm of EVERY runner, so the pass is byte-identical on Windows too. **This measures the standing under-a-percent policy correct at a ratio rather than by argument**: the reductions rejected on a Linux regression of ~0.2 pp are worth roughly 6x that in the other direction on the platform the encoder ships to. The `with_capacity` hint stays rejected everywhere — it removed ZERO allocations, so no allocator can rescue it. **CAVEAT: winperf's cell is 1024x1024 cq44 `--cpu-used 6` (~409k allocations), NOT the shipping cq27 s3 cell (~10.4M), and winperf builds `--no-default-features` so there is no libaom arm — this band sizes the PLATFORM RATIO, not the pass's absolute value at the shipping preset.** macOS remains unmeasured on this pass. **THE HOG DIVISION IS STRUCK FROM THE LEVER LIST — the exactness argument it was blocked on IS derivable, and having it shows the lever is not worth implementing.** `get_hist_bin_idx` computes `q = trunc((dy<<16)/dx)` then finds the smallest `idx` with `q <= BIN_THRESHOLDS[idx]`. Division-free form (truncation is floor for a non-negative quotient, ceil for a negative one, so it is sign-split): for `dx > 0`, `q <= T` iff `N < (T+1)*dx` when `N >= 0` and `N <= T*dx` when `N < 0`; mirrored for `dx < 0`. Verified at the boundaries where truncation bites (N=5/6,dx=2,T=2; N=-5,dx=2,T=-2/-3). **But `BIN_THRESHOLDS` reaches 441,831 and `|dx|` reaches 16,380 at bd12, so `T*dx` needs i64, and the lookup is a 4-way bisect PLUS a linear scan of up to 8 — trading ONE 20-40 cycle division for UP TO ELEVEN i64 multiplies.** A wash at best. **This is the session's own rule applied BEFORE writing code rather than after: it does not remove work, it RESHAPES it, and reshaping measured slower twice today.** What is left in `generate_hog` is that it reads u16 planes where libaom at bd8 reads u8 — the u16-at-bd8 root, not reachable from inside the function. **`optimize_txb_core` IS STILL NOT A LEVER, BUT THE RECORDED REASON IS WRONG AND THE RIGHT ONE IS MEASURED.** Every prior entry says 'do not touch, the trellis is FASTER than libaom' (1.13x; 417 ms vs C's 426). **That optimises the wrong objective** — clause (4) is ABSOLUTE encode time, and `optimize_txb_core` is **the port's LARGEST single symbol at 13.73 % = 417 ms**, where 5 % would be 21 ms, more than most landings this cycle. Checked properly: **(a) the cost is DIFFUSE — 1495 instructions, hottest single instruction 1.49 %, top twenty under 20 %**, spread over bounds checks against spilled slice lengths and stack-array reads; **(b) the oversized scratch that looked like the obvious win is COLD.** `let mut levels = [0u8; TX_PAD_2D]` compiles to a **1312-byte `memset` at entry**, sized for 32x32 whatever the transform — the exact KB-PERF-11 / KB-PERF-2-lever-3a shape KB-PERF-11 tells you to grep for — **but it carries 0.22 % of the symbol = ~1 ms**, because this runs per WINNING transform under `perform_block_coeff_opt`, not per candidate. **THE COUNT, NOT THE SHAPE, DECIDES.** So: leave it alone because its cost is diffuse and its scratch is cold, NOT because its ratio to libaom is good — a session ranking by ratio keeps skipping the biggest symbol in the port, and one ranking by milliseconds should find this note. **AND THE SAME TRANSFORMATION ON z1/z3 IS REJECTED, WHICH SHARPENS THE RULE: +0.123 %, 10/24, p=1.0000** against a +0.040 % null; byte-identical, reverted (band `.z1z3_rejected.tsv`). `z2_high` carries **78 ms** and lost **2 of 3** checks per pixel; `z3_high_scalar` carries **27 ms** and loses **1 of 3** (its store is STRIDED, so there is no row slice to take), and z1's scalar path barely clears the profile floor. Same shape, a quarter of the work removed, gone into the noise. **So 'removes work and adds none' is NECESSARY BUT NOT SUFFICIENT, and does not even fix the SIGN: of the session's four such changes, two measured negative (−0.827 %, −0.274 %) and two positive (−0.147 % null, +0.123 % null).** KB-PERF-44's *keep it anyway, it cannot be slower in principle* is therefore **not a predictor** — use it only to justify keeping a change that ALREADY measured negative, never to skip the band or to keep one that did not. **KB-PERF-55 — z2's LEFT-GATHER PREFIX PAID THREE BOUNDS CHECKS PER PIXEL: −0.274 %, 20/24, p=0.0015** against a −0.063 % null (`benchmarks/encoder_z2_left_prefix_2026-09-10.md`). Byte-identical on four cells; gate 1506/1506 twice. **It was selected BY THE RULE below, not by the ranking**: the question was not 'can z2 be vectorised' (KB-PERF-4 answered that — the left half is a true gather, `base_y` is not affine in `c`) but 'what is EXECUTED per pixel that need not be'. `perf annotate` put ~9 % of `z2_high` in bounds-check machinery alone (`cmpq` against a reloaded stack bound 6.08 %, the `movabsq $0x7fff...` signed-index limit 3.30 %). The prefix paid THREE checks per pixel — `left.at(base_y)`, `left.at(base_y + 1)` (each a checked index through an i32->usize cast) and the `dst[row + c]` store. Now one row slice for the destination (the vectorised tail becomes `&mut drow[c..]`) and ONE two-element window `&ld[i0..i0+2]` for the tap pair: three checks become one. **Exact including the panics** — `&ld[i0..i0+2]` panics exactly when `left.at(base_y+1)` would, both needing `i0+2 <= len`. Only `z2_high` is touched; z1/z3 and every scalar core keep `at()`, which keeps the bite proof asymmetric. **AND THE OBVIOUS FOLLOW-UP IS RULED OUT WITHOUT WRITING IT, 2026-09-10 — RECORDED SO IT IS NOT RE-CHASED.** The prefix's two remaining per-pixel multiplies (`x = (c<<6) - y*dx` and `y2 = (r<<6) - (c+1)*dy`) are both AFFINE in the loop counter, so replacing them with running accumulators looks like a textbook removal — the class that has paid every time this cycle. **It is not a removal: LLVM already strength-reduces them.** `objdump` of the shipped `z2_high` counts **7 `imul` in the WHOLE function body** (row-level and setup arithmetic, not two per pixel) against 175 `mov` and 37 `cmp`. The accumulator form would RESHAPE work that is already gone, i.e. the session's own null-or-slower class, and would have measured null exactly as `pixel_proj_error` did when its 'scalar' reduction turned out to be auto-vectorised. **The rule earned its keep twice now: disassemble BEFORE costing a lever off source that merely LOOKS like it does the work.** `perf annotate` still hangs on this ~30 MB binary (>240 s, no output), so `objdump` + opcode counting is the instrument to use. **A THIRD REJECTION, AND IT COMPLETES A PATTERN WORTH MORE THAN ANY ONE LEVER. Hoisting `aom_quantize_b_no_qmatrix`'s loop invariants measures +0.149 %, 8/24, p=1.0000** against a −0.045 % null — REVERTED. The invariants were real (both derived constants were recomputed per coefficient) and the hoist was byte-identical, but it **replaced a two-element indexed lookup with a BRANCH per iteration**, and the branch costs more than the load. **The session's four attempts sort cleanly: `#[inline]` on `intra_avail` +0.238 % (moved a call's cost, removed none); the quantize_b hoist +0.149 % (traded a load for a branch); the reconstruct round trip −0.147 % NULL (removed two copies, but only on a minority of txbs); the 4x4 variance direct read −0.827 % (removed a copy AND a walk on EVERY 4x4 unit). CHANGES THAT REMOVE WORK ON A HOT INNER LOOP PAY; CHANGES THAT RESHAPE HOW THE SAME WORK IS REACHED — inlining, hoisting, re-scoping a lookup — DO NOT, and measured SLOWER twice.** That is KB-PERF-44/45's allocation rule (removal pays, adding a mechanism does not) arriving independently from the arithmetic side. **KB-PERF-53 — THE ALLINTRA VARIANCE FACTOR COPIED 16 PIXELS INTO A SCRATCH TO MEASURE THEM: −0.827 %, 23/24 ROUNDS, p<0.0001** against a same-binary null of −0.048 % (`benchmarks/encoder_variance_4x4_2026-09-10.md`). Byte-identical on four cells; gate 1506/1506 twice. **The largest landing of the session and the second largest of the cycle.** `calc_normalized_variance_4x4`'s bd8 arm — the shipping path, called once per 4x4 unit of every block — copied the 16 samples into a `[u8; 16]` scratch through sixteen bounds-checked strided reads, then ran the GENERIC `dist::variance` over that scratch against a stride-0 ALL-ZERO reference, walking the same 16 values again. libaom calls `aom_variance4x4_sse2` on the plane. Replaced by a direct `sum`/`sum-of-squares` accumulation off the strided window, one bounds check per ROW (KB-PERF-37's pattern). **Bit-exact by arithmetic**: an all-zero reference makes `diff == a`, so the kernel reduces to `tsum=sum(a)`, `tsse=sum(a*a)`; `sum(a*a) <= 16*255^2` so its `wrapping_add` cannot wrap; the final expression is verbatim and by Cauchy-Schwarz never wraps either; and the `<= 255` bound the old `as u8` silently relied on is GUARANTEED by KB-51's `SampleRange` refusal, not assumed. **THE FINDING THAT UNLOCKED IT, AND IT IS A CORRECTION TO KB-PERF-47: that entry rejected `#[autoversion]` on `variance_raw` and explained it as variance's callers being 'inter/motion paths largely dead on an ALLINTRA KEY frame'. FALSE at the shipping preset — a frame-pointer attribution puts 80.9 % of `dist::variance` under `intra_rd_variance_factor` (the ALLINTRA visual-quality RD scale, maximally live on a KEY frame) and 9.1 % under `log_sub_block_var`, which calls the same helper.** KB-PERF-47's MEASUREMENT stands (that mechanism did fail); its EXPLANATION named the wrong callers, and the wrong explanation is what would have kept a +71.4 ms row closed. **When a perf rejection is recorded, the reason it failed is as load-bearing as the number — a wrong reason closes the row for everyone after you.** **THE FULL LEVER MAP AT THE SHIPPING PRESET, AND THE HEADLINE IS THAT THE NAMED LEVERS ARE ONLY 27 % OF WHAT THE BAR NEEDS — `benchmarks/encoder_lever_map_s3_2026-09-10.md`.** Fresh profile of both arms at 1024x1024 cq27 s3 (port 3040.11 / C 1549.47 / **1.9620x** / gap 1490.6 ms), every family summed like-for-like: memset+memmove +96.5 (7.29x), variance +71.4 (4.91x), *trellis +57.2 at 1.13x — DO NOT TOUCH*, dir-intra z2 +47.3 (2.28x), loopfilter +44.9 (4.67x), quantize_fp +41.2 (2.80x), *intra-CNN +37.6 — PARITY item*, dir-intra z1/z3 +34.8 (3.22x), **quantize_b +28.7 at 11.89x with NO SIMD AT ALL**, `intra_avail` +22.5 (no C counterpart), hog +21.1 (3.62x). **Closing EVERY named lever COMPLETELY is +408.5 ms = 27.4 % of the gap, and the bar needs 715.9 ms (48 %). So the 1.5x bar is NOT reachable by the named levers — a plan that lists them and stops is ~307 ms short.** That does not contradict the reachability record: its route is HALVING EVERY CLASS GAP (745 ms), i.e. breadth across the whole profile. What is new is the quantification — the named symbols carry about a quarter of the requirement. **THE ASYMMETRIC-PAIRING HAZARD BIT AGAIN AND WAS MATERIAL: reading C at `--percent-limit 0.10` gave variance 8.04x and loopfilter 12.30x; at 0.005 % they are 4.91x and 4.67x — ratios halved.** C size-specialises those families into ~20 kernels each and most fall under a top-N threshold while the port has ONE generic symbol that clears it. This is the third recorded instance (filter_intra, loopfilter, now these two). **Sum a FAMILY on both sides at a threshold low enough to include C's smallest variant, or the ratio is inflated by construction.** **AND A REJECTION WITH A RULE: `#[inline]` on `intra_avail` is SIGNIFICANTLY SLOWER — +0.238 %, 4/24 rounds faster, p=1.0000** against a −0.128 % null. `perf annotate` put its samples in a six-push prologue and a run of `movl 0xNN(%rsp)` reloading 21 stack-passed arguments, which reads as textbook call overhead; inlining a 206-instruction body at ten call sites costs more in I-cache and register pressure than the call. **`perf annotate` showing cost in a PROLOGUE or in ARGUMENT MARSHALLING does NOT imply inlining wins — those samples are where a call's unavoidable cost is ATTRIBUTED.** This cycle's three annotate-driven wins (KB-PERF-37/38/46) all replaced LOOP BODIES; both annotate-driven attempts at CALL OVERHEAD failed. **KB-PERF-51 — THE INTRA SEARCH RECONSTRUCTED THROUGH A SCRATCH IT DID NOT NEED. REMOVED, BYTE-IDENTICAL, AND IT MEASURES NULL: −0.147 % against a same-binary null of −0.077 %, BOTH 15/24, p=0.31** (`benchmarks/encoder_recon_in_place_2026-09-10.md`). Both intra walks did `tight.clear(); tight.extend_from_slice(pred); av1_inverse_transform_add(.., tight, txw, ..)` then a strided row copy back into `recon` — but **`recon[txb_off..]` ALREADY holds that txb's prediction** (luma stores it there under its own *"the C facade writes the prediction into dst"* comment; chroma's two arms write it there DIRECTLY and its `pred` is a copy taken back out) and `av1_inverse_transform_add` **takes a destination STRIDE**. C reconstructs straight into `pd->dst.buf` at `dst_stride`, so this is a fidelity alignment as well as a copy removal. Safety checked not assumed: `recon` is touched at exactly two points per loop body and `search_tx_type_intra_into` never receives it. **WHY IT WAS EXPECTED TO PAY AND WHY THAT WAS WRONG — §14 IN A FORM THIS LOG HAD NOT RECORDED.** A frame-pointer profile attributes **100 % of `__memmove_avx512`** to these two functions (`txfm_rd_in_plane_intra` 1.01 % ~ 30.7 ms, `txfm_rd_in_plane_uv_p` 0.39 % ~ 11.9 ms). **That is a CALLER attribution, not a COPY attribution**, and the caller holds THREE copies with different trip counts: the prediction store (every txb, load-bearing — C does it too), `winners` growth, and the `tight` round trip (only `best_eob > 0 && not_last_txb`). Only the third was removable. **Every previous §14 instance was a ranked STAGE or CLASS table; this is the frame-pointer instrument itself — the one introduced to make memory work actionable (KB-PERF-15) and used successfully twice since (KB-PERF-35, KB-PERF-37) — being optimistic the same way. Before costing a lever off a frame-pointer caller share, count how many distinct copies live in that caller and which your change actually removes.** **KEPT ANYWAY, on KB-PERF-44's rule and reported as a NULL rather than a win**: it strictly REMOVES work and adds none (one whole-block copy and one strided row copy per reconstructed txb), so it cannot be slower in principle, where the rejected variants all ADDED a mechanism. Byte-identical on all four cells; gate 1506/1506 twice. **Do not quote −0.147 % as a win.** Untouched and larger: the per-txb prediction store, whose removal means letting `predict_intra_high` write straight into `recon` at `ref_stride` (C's `ref == dst` form, already documented by KB-34) — a DSP signature change, not a caller change. **KB-PERF-48 — THE TLS SCRATCH POOL IS MERGED, ON A STATED POLICY: TAKE ALLOCATION REDUCTIONS THAT COST UNDER A PERCENT.** The reasoning is the user's call, not a measurement: **the drops COMPOUND, and locality only improves once the churn is broadly gone — so a per-change reject-on-any-regression rule is the WRONG STOPPING RULE for this class.** It would reject every step of a compounding sequence, and do so on the platform that systematically understates it. This change qualifies squarely: **−412,208 allocations (7,790,969 -> 7,378,761), +0.23 % on the understating platform, −0.600 % on `windows-11-arm` at 24/24, p<0.0001.** Byte-identical on four cells; gate 1506/1506 twice. **The `with_capacity` variant does NOT qualify and STAYS REJECTED — it removed ZERO allocations, so it is not a reduction at all, just two integer divisions per call added; it has nothing to compound.** Evidence: **KB-PERF-45's REJECTION IS PLATFORM-LOCAL AND HAS BEEN OVERTURNED ON WINDOWS — `benchmarks/encoder_tlspool_windows_2026-09-10.md`, run 34489034382.** The SAME byte-identical change: **Linux/glibc +0.234 % (slower, 6/24)** vs **`windows-11-arm` −0.600 % photo at 24/24 and −0.365 % detail at 23/24, BOTH p<0.0001**. **A ~0.8 pp swing with the SIGN REVERSED.** On Microsoft's heap the 412,208 removed allocations are worth more than the TLS + `RefCell` access costs; on glibc they are close to a free-list pop and are not. `windows-latest` x86-64 agrees in SIGN on both contents (−0.505 %, −0.668 %) but cannot resolve it (floor 0.59-1.65 % this run) — the same runner resolved the whole-cycle −7 % at 55x its floor. **This is the FIRST DIRECT confirmation in this repo of the platform effect KB-PERF-2 only inferred — measured on ONE source change both ways rather than across different levers.** **NOT MERGED**: branch `perf/tls-pool-windows-probe`, marked not-for-merge. It is a DECISION, not a measurement — adopt unconditionally (−0.6 % Win-ARM, +0.23 % Linux), `cfg`-gate it (byte-inert, but makes PERFORMANCE platform-dependent, which this project has avoided making true of its OUTPUT — KB-ARM-FLOAT root #1), or leave it rejected. Note the Linux regression is itself marginal (one of the two base-copy comparisons is p=0.54). **THE GENERAL LESSON: A PERFORMANCE REJECTION MEASURED ON ONE ALLOCATOR IS A REJECTION ON THAT ALLOCATOR.** Of this cycle's four allocation experiments, the two KEPT removed allocations with NO added mechanism (SmallVec inline storage, a stack array) and are platform-robust; the two REJECTED both ADDED a mechanism, and at least one is a win where the allocator is slower. **Re-run allocation rejections on `winperf.yml` before treating them as closed** — the remaining one (exact `with_capacity`, +0.77 %) removed ZERO allocations, so it has nothing to trade and needs no Windows run. Historical: **KB-PERF-45 — A THREAD-LOCAL SCRATCH POOL REMOVED 412,208 ALLOCATIONS AND MEASURED SLOWER (+0.269 %/+0.196 %; null −0.030 %). REVERTED.** The site was unambiguous — **4,822,422 of 7,790,969 allocations (62 %) are `finish_grow`**, all through `xform_quant_into` <- `xform_quant_optimize_split_into` <- `encode_intra_block_plane_{y,uv}` <- `encode_b_intra_dry`, because each plane walk built its own `XformQuantScratch::default()` and its buffers grew from empty on EVERY leaf. A `thread_local!` pool (taken at entry, put back at exit — total, since neither plane fn has an early return, and `encode_b_intra_dry` has 28 call sites so a threaded parameter is a large change) removed 412,208 of them and was still slower: **`POOL.with(..)` is a TLS lookup plus a `RefCell` borrow check, TWICE per plane call, ~1M of each per encode**, and dynamic TLS is a real function call on Linux — more than the malloc/free pairs it saved, which on glibc are close to a free-list pop. **THE RULE FOUR ATTEMPTS ESTABLISH — the outcome is predicted by WHAT REPLACES the allocation, not by how many are removed:** SmallVec/inline storage (1.5M removed, no mechanism added) **−0.457 %**; stack array (220k removed, nothing added) −0.074 %, kept; exact `with_capacity` (**0** removed, two divisions/call added) **+0.77 %**, reverted; TLS pool (412k removed, TLS+RefCell added) **+0.23 %**, reverted. **An allocation removal only pays if its replacement is CHEAPER THAN THE ALLOCATION WAS.** On glibc a hot reused size class is nearly free so the bar is low — inline storage and stack arrays clear it, TLS pools and extra arithmetic do not. **On Windows/macOS allocators the bar is much higher and these verdicts could FLIP, so `winperf.yml` should re-run the two REJECTED variants before anyone calls them dead.** **PLATFORM: the glibc number UNDERSTATES this** — KB-PERF-2 measured this class at 21 % of the win on Darwin and **86-99 % on Windows**, and glibc is close to a free-list pop on a hot reused size class, which is exactly the regime these 1.5M removed allocations were in. `winperf.yml`'s `arms: prepost` is the honest place to quote it. **NEXT: re-run heaptrack with `-C force-frame-pointers=yes` before picking the next allocation target** (release stacks come back unresolved and an unresolved stack invites a guess — that cost three wrong fixes). Prior record: **THE ALLOC PASS — ATTRIBUTION CORRECTED. The source is ONE function: `txfm_rd_in_plane_intra`, 823,028 temporary allocations of 1,359,984 (60.52 %), ~823k calls per 1 MP encode, and the allocation is the `winners: Vec<TxbWinner>` it RETURNS** (`TxbWinner` is three scalar fields, so it is the Vec, not its contents). KB-PERF-13 gave it an exact capacity and left it allocating BECAUSE it is returned. **The fix is a TWO-BUFFER KEEP-BEST SWAP, not a scratch** — the value escapes four layers (`uniform_txfm_yrd_intra` -> `choose_tx_size_type_from_rd_intra` -> `pick_uniform_tx_size_type_yrd_intra` -> `rd_pick_intra_sby_mode_y`) and at each level a candidate's winners must SURVIVE while losers' are discarded. **THREE WRONG FIXES WERE WRITTEN FIRST, and the check that caught all three is the cheapest one: the allocation count stayed IDENTICAL TO THE DIGIT (10,419,121).** `intra_mode_rd_eval` turned out to have NO CALLERS on the encode path; the capacity sites were single-element pushes. **Two rules: (a) a RELEASE build's heaptrack stacks can be UNRESOLVED, and an unresolved stack invites a guess — rebuild with `-C force-frame-pointers=yes` before attributing allocations (the same flag that made KB-PERF-15's memset callers visible); (b) verify the path is LIVE before fixing it.** **PLATFORM: worth more than the Linux number suggests** — glibc is fast on a hot reused size class, but **KB-PERF-2 measured this class of lever at 21 % of the win on Darwin and 86-99 % on Windows**, so 823k allocations/encode likely costs far more on the shipping platforms; `winperf.yml`'s `arms: prepost` can settle it once the refactor exists. Original record (heaptrack + a code reading) — `benchmarks/encoder_alloc_pass_2026-09-10.md`. ONE 1 MP ENCODE MAKES 10,419,121 ALLOCATIONS, and 5,870,590 of them (56 %) are `RawVecInner::finish_grow` — a Vec GROWING.** Source identified by reading: **two `Vec<i32>` per transform block**. `XformQuantScratch`'s `_into` forms reuse their buffers (KB-PERF-2) but **the OWNED `xform_quant_optimize`/`xform_quant` still allocate per call** and have live hot callers (`intra_rd.rs:181`, `encode_sb.rs:2063`/`:2071`, `lib.rs:858`/`:907`, `var_tx.rs:583`/`:603`), and **`TxbEncode` OWNS `qcoeff`/`dqcoeff`** as retained per-txb output. 5.87M/2 ~ 2.9M txbs with a growing pair each — the right order. **The fix is an ARENA (one flat `Vec<i32>` per walk with `(offset, len)` in `TxbEncode`), not a capacity hint.** **THE CAPACITY HYPOTHESIS WAS MEASURED AND REFUTED — and it is a rule worth keeping.** Exact `with_capacity` at the three sites heaptrack named left allocations **UNCHANGED at 10,419,121 (identical to the digit)** and measured **+0.77 % SLOWER** (+0.859 %/+0.673 %, **1 of 24 rounds faster**, p<0.0001; null +0.131 %). Two reasons: (1) **most blocks are single-txb**, so `Vec::new()` + one push allocates exactly once — the same as `with_capacity(1)`; there was no growth chain (temporaries fell by 13,922 of 1.93M); (2) **the capacity expression costs two integer divisions** (`div_ceil` twice) on a path called millions of times, to save allocations that did not exist. REVERTED. **A capacity hint is ARITHMETIC — on a hot path it must earn its keep; measure the allocation COUNT before and after, and if it does not move, the hint is pure cost.** **HONEST READING ON THE 6.7x `memory` CLASS: 10.4M allocations is genuinely bad and worth fixing, but it is NOT established as the perf problem** — the two biggest wins of the session (`highbd_subtract_block` −3.17 %, `block_error` −0.88 %) were ARITHMETIC loops found by disassembly, not allocation, and the arena's value is unmeasured because glibc is fast on a hot repeatedly-reused size class and these are all the same few sizes. **THE CYCLE WAS MEASURED ON WINDOWS AND IT TRANSFERS AT FULL STRENGTH — `benchmarks/encoder_winperf_cycle_2026-09-10.md`, run 34487059129.** `winperf.yml` `arms: prepost`, `b1463c8` -> HEAD, 24 rounds x 2 contents x 2 runners: **`windows-11-arm` −7.885 %/−7.939 %, `windows-latest` x86-64 −7.130 %/−7.677 %, ALL 24/24 rounds, p<0.0001**, each many multiples of its own same-binary noise floor (up to **55.8x** — that runner could not resolve sub-1 % effects for the whole project and resolves this one easily). **The same twelve landings compound to −7.22 % on Linux/glibc, so the platforms agree to well under a percentage point.** That was a REAL open question — every band this session ran on one Linux box with an unusually fast allocator, and KB-PERF-2 is on record with 21 % on Darwin vs 86-99 % on Windows for an allocation lever. **They agree because the MASS of this cycle is arithmetic and bounds-check work** (`highbd_subtract_block` −3.17 %, `block_error` −0.88 %, `sum_squares_2d_i16` −0.35 %, eight fused transform kernels) — none of it touches the allocator. **STILL UNSETTLED: the two REJECTED allocation variants (exact `with_capacity` +0.77 %, TLS pool +0.23 %) are NOT in this run** — it measures HEAD against a mid-cycle base and both were reverted — and they are exactly the shape the platforms disagree about; sizing them (and the arena) needs branches and a separate dispatch. Also: this is a **DELTA, not a ratio** (winperf builds `--no-default-features`, so there is no libaom arm and no Windows figure for clause (4) itself), on winperf's own ~293 ms synthetic contents rather than the 1 MP cell. **THE GAP IS NOW DIFFUSE: 1718 ms with no class over 26 %, and the 1.5x bar needs −946 ms = 55 % of it — not one class, let alone one kernel; the levers inside each class are 20-40 ms (0.6-1.2 % of the encode) rather than the 2 % kernels this cycle opened with.** Transform sequence remaining: 4x16 + 16x4 (1.66 % of inverses, 3.11 % of forwards at s3) — padded on one dimension like this pair, so at ~0.043 per share-point it is worth ~−0.2 pp, the smallest kernel yet and the first whose expected effect is near what a 30-round band can resolve; both dims >= 32 is ~0.6 % and not worth a kernel. Superseded: (branch on shape so the padded inverse uses whole-vector loads/stores with a tail); 4x16+16x4 (1.45 %/1.66 %); both dims >= 32 is ~0.6 % and not worth a kernel.** Superseded note: **REMAINING: 16x8 + 8x16 (9.02 % of forwards, 9.52 % of inverses) — rectangular 2:1 like these but BOTH dims >= 8, so NO padding and none of that scalar glue, which on the pattern should make them behave like the square kernels**; then 4x16+16x4 (1.45 %/1.66 %); both dims >= 32 is ~0.6 % and not worth a kernel. Superseded note: **NOW THE LARGEST REMAINING ITEM: the 4x8/8x4 INVERSE (15.16 % of inverses)**, with `NEW_INV_SQRT2` row scaling and its own `INV_SHIFT`; then 16x8+8x16 (9.02 % of forwards), then 4x16+16x4 (1.45 %). Both dims >= 32 is ~0.6 % and not worth a kernel. Superseded note: **WHAT IS LEFT AND IT IS THE LARGEST REMAINING SHARE: 8x4 + 4x8 are together 16.7 % of forwards, THREE TIMES 16x16's share**, untouched. Rectangular (`rect_type = ±1`, so `NEW_SQRT2` applies) with one 4-dimension, so one pass runs half-filled; the standalone `row_n == 4` arm measured NULL because an 8x8 transpose ran at full cost on half-zero data — **but inside a FUSED kernel the transpose is in registers either way, so that calculus changes.** Unmeasured, and the next thing to try. 32x32 and above are ~0.6 % of transforms and are not worth a kernel. Superseded note: **Still open in this sequence: the INVERSE 16x16 (5.04 % of inverses, `INV_SHIFT = [-2,-4]`), and 8x4+4x8 which are 16.7 % of forwards — MORE than 16x16 — but rectangular (`NEW_SQRT2`) with one 4-dimension, so one pass runs half-filled; the standalone `row_n == 4` arm measured NULL, though inside a fused kernel the transpose is in registers either way, so that calculus changes and is unmeasured.** **MEASURED IN A CLEAN WINDOW AFTER BOTH 8x8 LANDINGS: port 3376.96 ms / C 1537.5 ms = 2.196x at the shipping preset**, against **2.296x** at this cycle's start measured the same way (C twice, 0.27 % apart). **First reading below 2.2x.** Historical framing: **So the next structural landing is NOT the plane representation — it is a SIMD-PRESERVING size-specialised transform**, which the census's own correction already anticipated (*"a future 8x8 fusion would have to be VECTORISED itself to beat what is already there"*): 4x4 is 40.5 % of forwards and already fused, 8x8 is 25.7 % and is the target as a VECTOR kernel rather than the scalar fusion that measured +7.07 %. This does not make the plane refactor worthless (~12 % exceeds this whole cycle's three landings combined) — it makes it **not the largest item**, which the ranking had assumed. Historical claim follows: **The structural root under ~42 % of the gap is that the encoder holds planes as `u16` at every bit depth and so runs the HIGHBD kernels at bd8, where libaom runs a specialised lowbd path: forward transform +56.3 ms, inverse +51.0 ms, `highbd_variance` at bd8 +7.6 ms — one cause, ~115 ms, no single landing closes it.** **The obvious next step was BUILT AND MEASURED NULL — do not rebuild it.** The audited i16 inverse kernels are wired only to the DECODER's `u8` path (a reach probe on `try_inv_col_pass_u8` records ZERO calls); the u16-output twin was written, is CORRECT (427/427 still byte-identical, aom-dsp green), and is **-0.03 % on 5/8 interleaved rounds** — the -8 to -10 ms bound is refuted, measured 0. Mechanism, and it is KB-PERF-3's own lesson from the other side: an `i16x16` and an `i32x8` are both 256 bits, so lane count changes and work does not; the inverse kernels never get `fbtf16`'s cheaper `half_btf` because their 17-bit transients live in a two-domain i32 representation anyway. **Both transform rows therefore need a different idea than lane width** — the profile points at libaom's SIZE-SPECIALISED whole-transform functions vs the port's generic driver, which is a much larger programme. Two other hypotheses were tested and refuted the same day: inverse redundancy (C's full tail is 15.7 ms, ratio 4.2x, not the 17x a top-N cut suggested) and a `col_n >= 16` restriction (+0.4 %). **SIZE-CONTROLLED**: the 192x192 profile cell is L2-resident (~108 KiB of planes), which is exactly the regime where a traffic-halving lever cannot show a win, so the null was re-measured at 512x512 and 1024x1024 (3 MiB, L3) — **+0.64 % and +0.22 %, faster in 1/5 rounds each, byte-identical output**; the revert stands on a 28x pixel-count span. The STAGE RANKING transfers (max shift 3.0 pp, no reordering of the top four), so the cheap cell is a valid ranking tool at 24x the iteration speed — but **the ratio itself is cell-dependent — 2.4988x at 192x192 and 2.5717x at 1024x1024 after KB-PERF-7** (2.542x / 2.624x before it), so quote the headline with its cell. **KB-PERF-7 also measured the size regime from the other direction**: its four-pixel fold is WORTH MORE at 1024x1024 (-2.00 %) than at 192x192 (-1.68 %), which is what an L2-resident cell under-measuring a traffic lever looks like when the lever actually works. |
| (5) match the RD of C | byte identity is the strongest available evidence and holds on 427/427 standalone cells; the pinned divergences are the measured/attributed/bounded residual the directive permits to ship |
| (6) sensible conversion + wiring + testing of all of the C encoder | **PALETTE AND INTRABC ARE NOW WIRED INTO THE SHIPPING PATH — 2026-09-10, and the finding is that they were not.** `encode_key_frame` built its `PickFrameCfg` with `palette_costs: None` and `intrabc: None`, so it ran NEITHER screen-content search on ANY frame, including frames whose header it writes with `allow_screen_content_tools = 1` from its own detector. **No gate could see it: every byte gate in `self_contained_key_frame.rs` drives `shim_encode_av1_kf`, which hardcodes `enable_palette = 0, enable_intrabc = 0` (`dec_shim.c:612`), so the 427/427 is a parity claim against a palette-DISABLED libaom and was blind to both tools BY CONSTRUCTION.** That is KB-42's shape on a new axis — a gate can be green, exhaustive and honest about what it measures and still say nothing about a feature, because the ORACLE was configured out of the question. **Cost of the gap, libaom on BOTH sides so the number is the TOOLS' value and not the port's RD: −73.1 % bytes at 1024x768 cq20 s3, −68.6 % at cq32, −69.6 % at 512x384 cq20** (`benchmarks/encoder_screen_tools_2026-09-10.md`). Read the sign at the low-rate end: at cq44-55 palette ALONE is often WORSE (+21 % to +83 %) because a colour table plus an index map beats coarse transform coding only when the quantizer is fine; IntraBC carries those cells. **The matched-oracle gate found a STREAM-CORRUPTION bug on its first run:** `uncompressed_header` skips loop_filter/cdef/lr params entirely when `allow_intrabc` is set, the three sub-header structs each carry their own copy of the bit, and `derive_frame_header` hardcoded all three false — so the writer emitted three syntax elements the decoder never reads, desynchronising the tile group in the same OBU_FRAME. **Signature worth keeping: a CONSTANT +3 bytes over a byte-IDENTICAL tile payload (common suffix 31,114 of 31,132) — sign-random size-varying deltas are RD divergences; a constant delta with an identical payload is a header-length bug.** 51/54 -> 54/54. Gated by `screen_content_tools_byte_match_real_aomenc` (54 cells, matched `shim_encode_av1_kf_screen_content` oracle, non-vacuity asserted per cell by requiring the oracle's tools-ON stream to DIFFER from its own tools-OFF stream, plus a real-C decode round-trip) and 24/24 in the wider `screen_tools_gap` probe. The 427-cell gate now sets both knobs FALSE to match its own oracle — matching, not weakening: inert on every detector-negative cell, and on the `chk` cells it is what keeps it a parity test. **ONE DIVERGENCE, measured and bounded: IntraBC declines at coded-lossless.** C runs it there, dispatching the coeff arm to `av1_pick_uniform_tx_size_type_yrd` (tx_search.c:3824) instead of the recursive var-tx one; the port has no INTER uniform-tx arm and fired a `lossless forces TX_4X4` assertion — a crossing nothing had reached, since the shell never ran IntraBC and the bench never crossed it with cq 0. A lossless frame reconstructs to the source either way, so declining the SEARCH can only cost SIZE on cq-0 screen content, never a pixel, and it is a divergence rather than a refusal. Historical: `av1_determine_sc_tools_with_encoding` (PARITY C3) unported — **and MEASURED 2026-09-09 to be a DIVERGENCE, not a refusal, on the shipping path: 60/60 screen-shaped tiny cells encode through `encode_key_frame` with 0 refusals and 0 panics** (gated, `refusal_census::screen_shaped_tiny_cells_encode_rather_than_refuse`). The hard SCM assert is `aom-bench`'s differential harness, which no caller reaches. **The bd12 dispatch-tier disagreement is CLOSED 2026-09-09 (measured: both tiers byte-identical at `1920x1080 cq24 cpu0`) and now GATED by `bd12_dispatch_tier_agreement` — the tree had no bd12 coverage at all, which is why it sat open.** |



## Appendix — the original "three consequences" text from `CLAUDE.md` (as of 2984ad1)

Three consequences, because this REORDERS the queue below rather than adding to it:

1. **Breadth beats depth.** Porting, wiring and testing another piece of the C encoder
   outranks closing the last byte of an existing pin. Bitwise parity is the MEANS to
   RD-matching, not the end — when a divergence is measured, attributed to a named
   mechanism, bounded in reach and written down, it may SHIP. That is the stop rule the
   coverage queue never had, and it is what makes this finishable.
2. **Two classes still must close, and they are not "divergences".** (a) anything that
   REFUSES a configuration a caller can reach (`--deltaq-mode` 2/3 at `--cpu-used` >= 8
   asserts — **CLOSED 2026-09-08, KB-46**; the unported SCM trial — **MEASURED 2026-09-09
   and it is NOT a refusal on the shipping path**), because a refusal is a hole in the
   contract, not a byte. **The SCM reading needed a measurement because the two things that
   gap touches live in DIFFERENT crates.** `aom_bench`'s differential harness carries a hard
   `assert_eq!` naming `av1_determine_sc_tools_with_encoding` as the remaining C arm — but
   that is a HARNESS assertion, fired while comparing against libaom, and no caller reaches
   it. `aom_encode::key_frame::encode_key_frame` — the path zenavif calls — runs its own
   detector, lists the trial under "Not yet wired", and has a refusal surface of exactly
   `PlaneSize` / `Unsupported` / `Cancelled` / `SampleRange` / `AllocFailed` /
   `LimitExceeded`, none of them reachable from a screen-content decision. Measured on the
   tiny-cell class KB-41 named as the trial's own reproducer neighbourhood — 4 sizes x cq
   {40,44,50,57,62} x cpu {4,6,8} on content that is **100 % screen-shaped by libaom's own
   colour-count statistic** (10x its 10 % detector threshold): **60/60 ENCODE, 0 refused, 0
   panics**, in 1.46 s. Gated by `refusal_census::screen_shaped_tiny_cells_encode_rather_
   than_refuse`, which fails BY NAME if a refusal ever appears there and says that this
   moves the item back onto the must-close list. **So the SCM trial is a byte DIVERGENCE
   under the cap, and class (a) has no known open member on the public API.** The datagen
   fleet's 35 refused tiny cells are real, and they are `aom-bench`'s — a harness that
   legitimately refuses when it cannot model C's decision, not the backend zenavif selects; (b) ~~anything where the port disagrees with ITSELF across dispatch tiers (the bd12
   `1920x1080 cq24 cpu0` cell, +181 B default vs +55 B scalar), because a kernel whose
   tiers disagree is a differential hole (playbook §1)~~ — **(b) CLOSED 2026-09-09,
   MEASURED: both tiers now emit the byte-identical stream at that exact cell
   (`len 158731`, fnv `4b5f1efec0345f57`), and the residual `+59 B` against real aomenc is
   a plain divergence covered by the cap, not a differential hole. It closed silently
   because NOTHING in the tree encoded bd12; that hole is now gated by
   `bd12_dispatch_tier_agreement`.** So (a) — the refusals — is the only must-close class
   still open.
3. ~~**The encoder has none of the six zen cross-cutting contracts the decoder has.**~~
   **CLOSED 2026-09-08** — all six landed this cycle (limits + estimate, located/structured
   errors, `category()`/`is_transient()`, panic-freedom under a fuzz sweep, `AllocMode`,
   `enough::Stop` cancellation), gated by `encode_limits_and_estimate.rs`,
   `refusal_census.rs`, `configuration_support.rs`, `encode_cancel.rs` and
   `encode_fuzz_sweep.rs`. The one asymmetry that remains is that `whereat`-located errors
   have a decoder form and no encoder twin. Historical shape, kept because it is what the
   directive was written against: the encoder had `KeyFrameError` and nothing else — no
   limits, no cost estimate, no cancellation at all (the IntraBC DV search runs ~80 s on a
   1080p screenshot at cpu6 and could not be interrupted), no fuzz target, no allocation
   mode.

