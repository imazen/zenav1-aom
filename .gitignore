/target
/reference/libaom
/reference/extracted
/reference/*.log
.workongoing
.workongoing.decoder
conformance/data
conformance/vectors.json
__pycache__/
.claude/
.jj/

# local C-oracle checkout + build (never commit)
/c-ref/
/differential/shims/bin/

# cargo-fuzz build artifacts + working corpus/crashes (never commit)
crates/aom-decode/fuzz/target/
crates/aom-decode/fuzz/corpus/
crates/aom-decode/fuzz/artifacts/
crates/aom-decode/fuzz/coverage/
crates/aom-decode/fuzz/fuzz-*.log
local-corpus/
tools/avif-extract/target/
tools/avif-extract/Cargo.lock

# zenbench baselines are local measurement state, not repo content
.zenbench/

# xbench cross-encoder benchmark harness build output (own cargo workspace)
benchmarks/xbench/target/

# aom-target is its own cargo workspace (excluded from the root one because its
# zensim dev-dep is a path into a sibling repo), so it generates its own build
# output and lockfile. Same treatment as tools/avif-extract above.
crates/aom-target/target/
crates/aom-target/Cargo.lock
